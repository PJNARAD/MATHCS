import type { Concept } from '../types';

// ---------------------------------------------------------------------------
// GRAPH THEORY — 1: Basics, Paths, Trees, Euler/Hamilton, Bipartite, Coloring
// ---------------------------------------------------------------------------

export const gtBasics: Concept[] = [
  {
    id: 'graph-theory',
    title: 'Graph Theory',
    domain: 'graph-theory',
    topic: true,
    summary: 'The mathematics of networks: vertices and edges, and everything you can ask about connections. Graphs model the internet, maps, dependencies, social ties, and computation itself.',
    level: 'core',
    csFields: ['algorithms-dsa', 'computer-networks', 'distributed-systems', 'databases', 'competitive-programming'],
    prerequisites: ['sets'],
    related: ['graph-definition', 'bfs-dfs', 'trees', 'dijkstra'],
    next: ['graph-definition'],
    content: [
      { t: 'intuition', text: 'A graph abstracts a network to its essence: points (vertices) and links (edges). Strip the geography from a map and you have a graph; strip the semantics from a dependency system and you have a graph. The surprising fact is that the stripped structure retains almost everything that matters — distances, bottlenecks, cycles, partitions, and optimal routes — all of which become questions with clean mathematical answers.' },
      { t: 'cs', items: [
        { area: 'Networks', how: 'Routers are vertices, links are edges; routing protocols compute shortest paths in this graph.' },
        { area: 'Dependencies', how: 'Build systems, package managers, and task schedulers are DAGs (directed acyclic graphs).' },
        { area: 'Databases', how: 'Graph databases and query-plan optimizers reason about edge structure directly.' },
        { area: 'Social & recommender systems', how: 'Friendship, citation, and purchase graphs drive PageRank-style link analysis.' },
      ] },
    ],
    practice: [],
  },
  {
    id: 'graph-definition',
    title: 'Graph Definition & Terminology',
    domain: 'graph-theory',
    parent: 'graph-theory',
    summary: 'A graph G = (V, E): vertices, edges, degree, and the full vocabulary — simple vs multigraph, directed vs undirected, weighted, paths, neighbors.',
    level: 'core',
    csFields: ['algorithms-dsa', 'computer-networks', 'databases'],
    prerequisites: ['graph-theory', 'sets'],
    related: ['graph-types', 'degree-and-handshake', 'paths-walks-cycles'],
    next: ['graph-types'],
    content: [
      { t: 'def', title: 'Graph', text: 'A (simple undirected) graph is $G = (V, E)$ with a finite vertex set $V$ and an edge set $E \\subseteq \\{\\{u,v\\} : u, v \\in V,\\; u \\ne v\\}$. Write $uv \\in E$ or $u \\sim v$ when $u$ and $v$ are **adjacent** (neighbors); the neighbors of $u$ form $N(u)$.' },
      { t: 'props', title: 'Core vocabulary', items: [
        { title: 'Directed (digraph)', text: 'Edges are ordered pairs $uv \\in E \\subseteq V \\times V$; $u$ is the tail, $v$ the head. Written $u \\to v$.' },
        { title: 'Weighted', text: 'A function $w: E \\to \\mathbb{R}$ assigns lengths/costs; the weight of a path is the sum of its edges.' },
        { title: 'Degree', text: '$\\deg(u) = |N(u)|$ — the number of edges incident to $u$ (in a digraph: out-degree and in-degree separately).' },
        { title: 'Path / walk', text: 'A **walk** repeats vertices freely; a **path** has all vertices distinct; a **cycle** is a closed path (first = last vertex only).' },
        { title: 'Distance', text: '$d(u, v)$ = length of the shortest path between u and v; $\\infty$ if none exists.' },
      ] },
      { t: 'ex', title: 'A small graph, fully described', steps: [
        'V = {A, B, C, D, E}; E = {AB, AC, BD, CD, DE}.',
        'Degrees: deg(A)=2, deg(B)=2, deg(C)=3, deg(D)=3, deg(E)=1.',
        'A–C–D–E is a path of length 3; A–B–D–C–A is a cycle of length 4.',
        'd(A, E) = 3 via A–C–D–E.',
      ] },
      { t: 'viz', id: 'graph-editor', props: { preset: 'basic' } },
      { t: 'analogy', title: 'Think of it like this', text: 'A city map: intersections are vertices, streets are edges. The question “how far is it?” is a graph question; “what is the fastest route?” adds weights; “can I visit every street exactly once?” is a graph theorem in waiting.' },
      { t: 'cs', items: [
        { area: 'Adjacency representations', how: 'Adjacency list (O(V+E), sparse-friendly) vs adjacency matrix (O(1) edge test, dense-friendly) — the two ways every graph algorithm stores its input.' },
        { area: 'Graph databases', how: 'Neo4j-style stores are literally (V, E) with properties; traversals are path queries.' },
      ] },
    ],
    practice: [
      { id: 'gd-p1', q: 'In the graph with V = {1,2,3,4,5,6} and E = {12, 13, 23, 24, 45, 46, 56}, find the degrees and name one cycle.', type: 'short', diff: 'easy',
        answer: 'deg: 1→2, 2→3, 3→2, 4→3, 5→2, 6→2. Cycle: 2–3–… wait, 23 is an edge but 3–? no edge from 3 to 4. Cycles: 2–4–5–6–4–2? 45,46,56: 4–5–6–4 is a triangle (edges 45, 56, 46 ✓). Also 1–2–3–1 (12, 23, 13 ✓).',
        explain: 'Triangles: {1,2,3} and {4,5,6}; the bridge 2–4 connects them.',
      },
      { id: 'gd-p2', q: 'Is the empty graph (no edges) on 5 vertices connected? Justify with the definition.', type: 'short', diff: 'easy',
        answer: 'No (for n > 1): connectivity requires a path between every pair, but no path exists between any two distinct vertices when there are no edges.',
        explain: 'Connectivity is defined by paths; no edges means no paths.',
      },
    ],
  },
  {
    id: 'graph-types',
    title: 'Graph Types: Complete, Regular, Bipartite, Planar',
    domain: 'graph-theory',
    parent: 'graph-theory',
    summary: 'The named families of graphs — complete Kₙ, n-regular, bipartite, planar, trees — and the quick tests that identify each.',
    level: 'core',
    csFields: ['computer-networks', 'algorithms-dsa', 'theoretical-cs'],
    prerequisites: ['graph-definition'],
    related: ['bipartite-graphs', 'planarity', 'trees'],
    content: [
      { t: 'table', title: 'Families', head: ['Family', 'Definition', 'Key fact'], rows: [
        ['Complete $K_n$', 'every pair adjacent', '$\\binom{n}{2}$ edges'],
        ['$n$-regular', 'every vertex degree $n$', 'handshake: $n|V|$ even'],
        ['Bipartite', 'V splits into two sets, edges only across', 'no odd cycles'],
        ['Planar', 'drawn in the plane without crossings', '$|E| \\le 3|V| - 6$ (for $|V| \\ge 3$)'],
        ['Tree', 'connected, acyclic', '$|E| = |V| - 1$'],
        ['Clique', 'mutually adjacent set of vertices', 'a $K_k$ inside G'],
      ] },
      { t: 'ex', title: 'Identifying families', steps: [
        'The friendship graph on 4 people where everyone is friends with exactly 2 others: 2-regular on 4 vertices — either a 4-cycle or two disjoint edges.',
        'A round-robin tournament on 8 teams: complete graph K₈, 28 games.',
        'A utility network with power lines on one side and houses on the other: bipartite by construction.',
      ] },
      { t: 'cs', items: [
        { area: 'Mesh networks', how: 'A full-mesh of n nodes is Kₙ — n(n−1)/2 links, the reason large networks are not full mesh.' },
        { area: 'Scheduling', how: 'Course-conflict graphs (courses = vertices, shared students = edges) are colored: colors = time slots. Bipartite cases (day/night) color in 2 colors.' },
      ] },
    ],
    practice: [
      { id: 'gt-p1', q: 'How many edges does K₁₂ have?', type: 'numeric', diff: 'easy', answer: '66',
        explain: 'C(12,2) = 12·11/2 = 66.',
      },
      { id: 'gt-p2', q: 'Can a simple graph on 6 vertices be 4-regular? If so, how many edges?', type: 'short', diff: 'medium',
        answer: 'Yes: the complement of the 6-cycle… simplest: K₆ is 5-regular; delete a perfect matching (3 disjoint edges) → 4-regular with 9 edges. Handshake: 6·4/2 = 9 ✓.',
        explain: 'Existence: n-regular graphs exist for all 0 ≤ n ≤ |V|−1 except when n·|V| is odd (handshake).',
      },
    ],
  },
  {
    id: 'degree-and-handshake',
    title: 'Degree & the Handshake Lemma',
    domain: 'graph-theory',
    parent: 'graph-theory',
    summary: 'Sum of degrees = 2·(number of edges) — the handshake lemma, and its immediate corollaries: the number of odd-degree vertices is even.',
    level: 'core',
    csFields: ['algorithms-dsa', 'theoretical-cs', 'competitive-programming'],
    prerequisites: ['graph-definition'],
    related: ['euler-paths', 'graph-coloring'],
    content: [
      { t: 'thm', name: 'Handshake lemma', statement: 'For every undirected graph, $\\sum_{v \\in V} \\deg(v) = 2|E|$.',
        proofTitle: 'Proof (counting edges two ways)',
        proof: [
          'Count the pairs (v, e) where v is an endpoint of e.',
          'By vertex: each vertex v contributes deg(v) pairs — total Σ deg(v).',
          'By edge: each edge has exactly two endpoints — total 2|E|.',
          'Same set counted twice: Σ deg(v) = 2|E|. ∎',
        ] },
      { t: 'thm', name: 'Even number of odd-degree vertices', statement: 'Every graph has an even number of vertices of odd degree.',
        proofTitle: 'Proof',
        proof: [
          'Σ deg(v) = 2|E| is even.',
          'The sum of even degrees is even, so the sum of the odd degrees must be even.',
          'A sum of odd numbers is even iff there are an even number of them.',
        ] },
      { t: 'ex', title: 'Applications', steps: [
        '“Five people at a party each shake hands with an odd number of the others” — impossible: the handshake graph on the 5 would have 5 odd vertices.',
        'A network where every router has degree 5: the number of routers must be even (handshake: 5n = 2m).',
        'A graph with degree sequence 3, 3, 3, 3, 2: sum = 14 = 2·7 — plausible (realizable as two triangles sharing an edge… e.g. K₄ minus one edge plus a leaf attached: degrees 3,3,3,2,? — constructively: K₄ is 3,3,3,3; replace one vertex’s degree by attaching a leaf: 3,3,3,2,1 sums to 12 = 6 edges — a different sequence. The point: the handshake test is necessary, not sufficient (the Havel–Hakimi algorithm decides realizability).',
      ] },
      { t: 'cs', items: [
        { area: 'Network design', how: 'Symmetrical topologies (every node same degree) are checked by regularity; asymmetric ones must still satisfy the handshake identity — a cheap invariant for topology validation tools.' },
        { area: 'Social data', how: '“Most users have an odd number of connections” in a dataset with n users is a red flag: it violates the handshake lemma, so the data is corrupted or directed (in/out degrees differ).' },
      ] },
    ],
    practice: [
      { id: 'dh-p1', q: 'A graph has 7 vertices with degrees 1, 2, 3, 4, 5, 6, 7. Is it possible?', type: 'mcq', diff: 'easy',
        options: ['Yes', 'No — sum is odd', 'No — the degree-7 vertex must connect to the degree-1 vertex twice', 'Only if multiedges allowed'], correct: 1,
        explain: 'Sum = 28 = even — that passes! But the degree-7 vertex connects to all 6 others, so the degree-1 vertex’s only neighbor is the degree-7 one… then the degree-2 vertex also needs a neighbor among the remaining 5 — possible? The real obstruction: the degree-6 vertex connects to everyone except one vertex; the degree-1 vertex must be adjacent to the degree-7 vertex; hence the degree-6 vertex’s missing neighbor must be… the degree-1 vertex? But degree-1 has only one neighbor (degree-7). Then degree-6 is adjacent to everyone else (5 neighbors) — contradiction, it needs 6. So impossible. Answer: No (option 1 “sum odd” is wrong — the sum is even; the correct reason is the adjacency contradiction).',
        mistake: 'The sum being 28 (even) passes the handshake test — the obstruction is structural, which is why the handshake lemma is necessary but not sufficient.',
      },
      { id: 'dh-p2', q: 'How many edges does a 3-regular graph on 12 vertices have?', type: 'numeric', diff: 'easy', answer: '18',
        explain: '3·12 = 2·|E| → |E| = 18.',
      },
    ],
  },
];

export const gtPaths: Concept[] = [
  {
    id: 'paths-connectivity',
    title: 'Paths, Walks & Connectivity',
    domain: 'graph-theory',
    topic: true,
    summary: 'The difference between walks, paths, and cycles — and connectivity: when is a graph in one piece, and what happens when an edge (or vertex) is removed?',
    level: 'core',
    csFields: ['computer-networks', 'algorithms-dsa', 'distributed-systems'],
    prerequisites: ['graph-definition'],
    related: ['paths-walks-cycles', 'connectivity', 'euler-paths'],
    next: ['paths-walks-cycles'],
    content: [
      { t: 'intuition', text: 'Connectivity is the graph-theoretic “is the system in one piece?” A network stays functional while its graph is connected; a single edge failure that disconnects it is a bridge, and a single node failure is an articulation point. These are not hypotheticals — they are exactly the failure modes that redundancy designs (ring networks, k-connected topologies) engineer against.' },
      { t: 'cs', items: [
        { area: 'Fault tolerance', how: 'A k-connected network survives any k−1 node failures — the metric data-center designers optimize.' },
        { area: 'Distributed systems', how: 'A partition (disconnection of the message graph) is precisely the “P” in CAP.' },
      ] },
    ],
    practice: [],
  },
  {
    id: 'paths-walks-cycles',
    title: 'Walks, Paths, Cycles & Distance',
    domain: 'graph-theory',
    parent: 'paths-connectivity',
    summary: 'Precise definitions: walk (repeats allowed), path (no repeated vertices), cycle (closed path), trail (no repeated edges) — and graph distance as shortest-path length.',
    level: 'core',
    csFields: ['algorithms-dsa', 'computer-networks', 'competitive-programming'],
    prerequisites: ['graph-definition'],
    related: ['connectivity', 'bfs-dfs', 'euler-paths'],
    next: ['connectivity'],
    content: [
      { t: 'table', title: 'The four objects', head: ['Object', 'Vertices repeat?', 'Edges repeat?', 'Example'], rows: [
        ['Walk', 'yes', 'yes', 'A–B–C–B–D'],
        ['Trail', 'yes', 'no', 'A–B–C–B–D with distinct edges'],
        ['Path', 'no', 'no (automatic)', 'A–B–C–D'],
        ['Cycle', 'only first=last', 'no', 'A–B–C–D–A'],
      ] },
      { t: 'def', title: 'Distance & diameter', text: '$d(u,v)$ = minimum length of a u–v path ($\\infty$ if none). The **diameter** of a connected graph is $\\max_{u,v} d(u,v)$; the **radius** is the minimum, over all vertices, of the vertex’s maximum distance (a center).' },
      { t: 'ex', title: 'Distances in a small graph', steps: [
        'Path graph P₄: 1–2–3–4. d(1,4) = 3 = diameter.',
        'Cycle C₆: d(u,v) = min(|i−j|, 6−|i−j|); diameter = 3 (opposite vertices).',
        'Star K₁,₅: diameter = 2 (leaf to leaf through the center).',
      ] },
      { t: 'props', items: [
        { title: 'Uniqueness', text: 'A graph with exactly one path between any two vertices is a tree (see Trees).' },
        { title: 'Even cycles', text: 'A bipartite graph has no odd cycles — and no odd cycle is the exact obstruction (a graph is bipartite iff it has no odd cycle).' },
        { title: 'Small-world', text: 'Real social networks have small diameter (the “six degrees” phenomenon) — a structural fact about the graph, not the labels.' },
      ] },
      { t: 'cs', items: [
        { area: 'Routing', how: 'd(u,v) in hops is the metric routing tables minimize; diameter bounds worst-case latency in a network of equal-speed links.' },
        { area: 'Search', how: 'BFS computes all d(s, ·) in one run — the graph’s distance map from a source (see BFS/DFS).' },
      ] },
    ],
    practice: [
      { id: 'pw-p1', q: 'In the 5-cycle C₅, what is the diameter? And in C₆?', type: 'numeric', diff: 'easy', answer: 'C₅: 2; C₆: 3',
        explain: 'C₅: opposite-ish vertices are 2 apart (floor(5/2)). C₆: opposite vertices are 3 apart.',
      },
      { id: 'pw-p2', q: 'Prove: if a connected graph has exactly one path between every pair of vertices, then it is a tree (acyclic with |E| = |V|−1).', type: 'proof', diff: 'hard',
        answer: 'Acyclic: a cycle C with vertices v₁…vₖ gives two distinct v₁–v₂ paths (the edge v₁v₂ and the rest of the cycle) — contradiction. |E| = |V|−1: do a DFS/BFS from any root; every non-root vertex is reached by exactly one edge (its unique path’s last edge), giving |V|−1 tree edges; if a further edge existed it would create a second path between two vertices (or a cycle) — contradiction. Conversely, a tree has exactly one path between any pair: two distinct paths would form a cycle.',
        explain: 'The unique-path property pins down the edge count exactly — the bridge between path language and the tree formula.',
      },
    ],
  },
  {
    id: 'connectivity',
    title: 'Connectivity, Bridges & Articulation Points',
    domain: 'graph-theory',
    parent: 'paths-connectivity',
    summary: 'Connected components, bridges (edges whose removal disconnects), articulation points (vertices whose removal disconnects), and k-connectivity as a fault-tolerance measure.',
    level: 'core',
    csFields: ['computer-networks', 'distributed-systems', 'algorithms-dsa'],
    prerequisites: ['paths-walks-cycles'],
    related: ['bfs-dfs', 'trees', 'network-flow'],
    content: [
      { t: 'def', title: 'Definitions', text: 'G is **connected** if every pair of vertices has a path between them. A **component** is a maximal connected subgraph; the component partition is computed by BFS/DFS. A **bridge** is an edge whose removal increases the number of components. An **articulation point** (cut vertex) is a vertex whose removal does the same.' },
      { t: 'props', items: [
        { title: 'Bridges in DFS', text: 'An edge is a bridge iff it is a tree edge in a DFS whose subtree contains no back edge to a strict ancestor of its parent — the classic low-link algorithm finds all bridges in O(V+E).' },
        { title: 'Articulation points', text: 'A DFS root is an articulation point iff it has ≥ 2 children; a non-root v is one iff some child’s subtree cannot reach an ancestor of v (the same low-link machinery).' },
        { title: 'k-connectivity', text: 'G is k-connected if removing fewer than k vertices leaves it connected. k = 1: connected; k = 2: no articulation point; k = 3: no pair whose removal disconnects. Max k ≤ Δ(G) (the minimum degree).' },
      ] },
      { t: 'ex', title: 'A ring with a chord', steps: [
        'Graph: cycle 1–2–3–4–1 plus chord 1–3.',
        'Bridges: none (every edge lies on a cycle).',
        'Articulation points: none (remove any vertex: 1–2, 2–3, 3–4 remain connected via remaining edges… check vertex 3: left with 1–2, 1–4, 2? edges: 1–2, 4–1, 1–3 gone, 2–3 gone, 3–4 gone → 1–2 and 1–4 → still connected. Yes, 2-connected.)',
        'Connectivity k = 2: removing {1, 3} leaves 2 and 4 disconnected.',
      ] },
      { t: 'cs', items: [
        { area: 'Redundancy', how: 'Dual-homed servers (two uplinks) create 2-connected attachments — no single switch is an articulation point for that server.' },
        { area: 'Consensus', how: 'Gossip protocols converge fastest on expander graphs (high connectivity, small diameter) — a graph-theoretic design goal.' },
      ] },
    ],
    practice: [
      { id: 'conn-p1', q: 'How many connected components does the graph with V = {1..6} and E = {12, 13, 45, 46} have? List them.', type: 'short', diff: 'easy',
        answer: 'Two components: {1, 2, 3} and {4, 5, 6}.',
        explain: 'No edge crosses between the two triples.',
      },
      { id: 'conn-p2', q: 'Which vertices are articulation points in a path P₄ (1–2–3–4)?', type: 'short', diff: 'easy', answer: '2 and 3. Removing 2 isolates 1; removing 3 isolates 4. The endpoints 1, 4 are not articulation points.',
        explain: 'Internal vertices of a path are all cut vertices; endpoints are leaves.',
      },
    ],
  },
];

export const gtTrees: Concept[] = [
  {
    id: 'trees',
    title: 'Trees & Spanning Trees',
    domain: 'graph-theory',
    topic: true,
    summary: 'Connected, acyclic graphs — the minimal connected structure. Equivalent characterizations, rooted trees, spanning trees, and Cayley’s formula: a connected graph on n vertices has n−1 edges.',
    level: 'core',
    csFields: ['algorithms-dsa', 'computer-networks', 'compilers', 'distributed-systems'],
    prerequisites: ['connectivity'],
    related: ['spanning-trees', 'minimum-spanning-trees', 'recursion'],
    next: ['spanning-trees'],
    content: [
      { t: 'thm', name: 'Tree characterizations', statement: 'For a graph on n ≥ 1 vertices, the following are equivalent: (1) connected and acyclic; (2) connected with n−1 edges; (3) acyclic with n−1 edges; (4) any two vertices joined by exactly one path.',
        proofTitle: 'Proof (cycle of implications)',
        proof: [
          '(1)→(2): take a spanning DFS/BFS tree — it uses n−1 edges (one per non-root vertex), and connectedness gives no more edges than the spanning tree plus… precisely: every non-root vertex contributes exactly one tree edge; an extra edge would form a cycle (contradicting acyclic), so exactly n−1.',
          '(2)→(4): connected gives a path; two distinct paths between u, v would create a cycle, using at least one non-tree edge — but a connected n−1-edge graph has no room for one (adding it gives n edges and a cycle… more directly: a connected graph has a spanning tree of n−1 edges; the whole graph has n−1 edges, so it equals its spanning tree and is a tree: (4) by (1)↔).',
          '(4)→(3): unique paths ⇒ no cycle (a cycle gives two paths). Count edges: sum over vertices of (edges to parent in a BFS tree) = n−1; a further edge would duplicate a path — so n−1 total.',
          '(3)→(1): acyclic n−1-edge graph: adding any missing edge between different components creates no cycle (no path existed) — so it must have exactly one component, i.e. connected. (Equivalently: an acyclic component on k vertices has ≤ k−1 edges, with equality iff connected; summing gives ≤ n − c for c components, so n−1 forces c = 1.)',
        ] },
      { t: 'p', text: 'A **spanning tree** of G is a tree containing all vertices of G (a connected acyclic edge subset). Every connected graph has at least one — take any BFS/DFS tree. The number of spanning trees of the complete graph Kₙ is $n^{n-2}$ (Cayley’s formula) — the count that underlies network reliability calculations.' },
      { t: 'ex', title: 'Rooted trees in CS', steps: [
        'A file system: directories and files form a rooted tree (the root is /); every file has a unique path from the root — exactly the tree’s unique-path property.',
        'A DOM: the document tree; CSS selectors navigate parent/child/ancestor — tree operations.',
        'A process tree: every process has a parent (its spawner); PID 1 is the root.',
      ], result: '“Unique path from root” is the property users feel; “n−1 edges” is the property the data structure exploits.' },
      { t: 'cs', items: [
        { area: 'Syntax trees', how: 'A program’s abstract syntax tree (AST) is a rooted tree; compilation is structural recursion over it.' },
        { area: 'Spanning trees in networks', how: 'The Spanning Tree Protocol (STP) builds a loop-free spanning tree of the LAN — exactly the “one path between any two switches” guarantee.' },
        { area: 'Union–find', how: 'The disjoint-set structure maintains a forest; “is a–b connected?” is answered in near-constant time via path compression.' },
      ] },
    ],
    practice: [
      { id: 'tree-p1', q: 'How many edges does a tree on 100 vertices have? How many spanning trees does K₄ have?', type: 'numeric', diff: 'easy', answer: '99 edges; Cayley: 4^{4−2} = 16 spanning trees of K₄',
        explain: 'n−1 edges for any tree; n^{n−2} for Kₙ.',
      },
      { id: 'tree-p2', q: 'Prove: removing any edge from a tree disconnects it (i.e., every tree edge is a bridge).', type: 'proof', diff: 'medium',
        answer: 'Let e = uv. In a tree T, the unique u–v path is the edge e itself (a longer u–v path plus e would be a cycle). Remove e: no u–v path remains (uniqueness), so T−e is disconnected.',
        explain: 'Tree edges are exactly the bridges — the dual of “every non-tree edge closes a cycle.”',
      },
    ],
  },
  {
    id: 'spanning-trees',
    title: 'Spanning Trees: Existence & Construction',
    domain: 'graph-theory',
    parent: 'trees',
    summary: 'Every connected graph contains a spanning tree — and BFS/DFS construct one in O(V+E). Spanning trees are the backbone of loop-free networking and the starting point for minimum-spanning-tree algorithms.',
    level: 'core',
    csFields: ['computer-networks', 'algorithms-dsa', 'distributed-systems'],
    prerequisites: ['trees'],
    related: ['minimum-spanning-trees', 'bfs-dfs'],
    next: ['minimum-spanning-trees'],
    content: [
      { t: 'thm', name: 'Existence', statement: 'Every connected graph has a spanning tree.',
        proofTitle: 'Proof',
        proof: [
          'Start with all edges; while a cycle exists, remove one edge of that cycle.',
          'Removing a cycle edge never disconnects the graph (the cycle’s other edges still connect its endpoints).',
          'The process ends (edges decrease), leaving a connected, acyclic graph spanning all vertices: a spanning tree.',
          '(Equivalently: run BFS — its tree edges form a spanning tree.)',
        ] },
      { t: 'ex', title: 'BFS vs DFS spanning trees', steps: [
        'Same graph, different trees: BFS spanning trees minimize distance from the root (each vertex is reached at its true distance level); DFS spanning trees tend to be “deep” (long chains).',
        'The choice changes the tree’s shape but not its edge count (always n−1).',
        'STP in Ethernet elects one spanning tree from the switch topology — the protocol is a distributed implementation of “build me a spanning tree.”',
      ] },
      { t: 'cs', items: [
        { area: 'Loop prevention', how: 'Broadcasts explode without a spanning tree (a frame circles the ring forever). STP breaks every cycle by disabling one edge per cycle — literally, a spanning tree is enforced.' },
        { area: 'Broadcasting', how: 'Sending a message along a spanning tree reaches every node exactly once — the standard multicast backbone in software-defined networks.' },
      ] },
    ],
    practice: [
      { id: 'st-p1', q: 'A connected graph has 8 vertices. How many edges does any spanning tree of it have?', type: 'numeric', diff: 'easy', answer: '7',
        explain: 'Any tree on 8 vertices has 7 edges.',
      },
      { id: 'st-p2', q: 'Why does the “remove cycle edges” process always terminate with a connected graph?', type: 'short', diff: 'medium',
        answer: 'Termination: the edge count strictly decreases and is bounded below. Connectivity: removing an edge that lies on a cycle never disconnects — its two endpoints remain connected via the cycle’s other edges, and everything else was connected to both before.',
        explain: 'Both invariants are what make the greedy “peel the cycles” construction valid.',
      },
    ],
  },
  {
    id: 'minimum-spanning-trees',
    title: 'Minimum Spanning Trees',
    domain: 'graph-theory',
    parent: 'trees',
    summary: 'The cheapest spanning tree: Kruskal (sort edges, union–find) and Prim (grow from a root). Optimal by the cut property — and the model for wiring, clustering, and network design.',
    level: 'advanced',
    csFields: ['algorithms-dsa', 'computer-networks', 'competitive-programming', 'data-science'],
    prerequisites: ['spanning-trees'],
    related: ['dijkstra', 'kruskal-prims', 'union-find'],
    content: [
      { t: 'def', title: 'MST', text: 'For a connected weighted graph, a **minimum spanning tree** is a spanning tree of minimum total edge weight. An MST always exists; it is unique iff all edge weights are distinct.' },
      { t: 'thm', name: 'Cut property', statement: 'Let C be any cut of the vertices. The lightest edge crossing C (if unique) belongs to every MST.',
        proofTitle: 'Proof',
        proof: [
          'Let e be the unique lightest edge across cut C, and let T be any MST not containing e.',
          'Add e to T: a cycle forms, and that cycle contains another edge f crossing C (it must reconnect the two sides).',
          'By choice of e, w(e) < w(f). Swap: T′ = T − f + e is a spanning tree of smaller weight — contradicting T’s minimality.',
          'So e is in every MST.',
        ] },
      { t: 'h', text: 'Kruskal’s algorithm' },
      { t: 'p', text: 'Sort all edges by weight; scan them, adding an edge iff it connects two different components (union–find). The cut property justifies each addition: the new edge is the lightest across the cut separating the component from the rest of its future. Complexity: O(E log E) (the sort dominates; union–find is near-constant).' },
      { t: 'h', text: 'Prim’s algorithm' },
      { t: 'p', text: 'Grow one tree from a start vertex: repeatedly add the lightest edge from the tree to outside it. With a binary heap: O(E log V). Justification: each added edge is lightest across the cut (tree, rest) — the cut property again. Dijkstra’s algorithm is the same growth process with distances instead of edges — the famous structural twin (see Dijkstra).' },
      { t: 'ex', title: 'Kruskal on a small graph', steps: [
        'V = {A,B,C,D}; edges: AB=1, CD=2, BC=3, AD=4, AC=5.',
        'Sorted: 1(AB), 2(CD), 3(BC), 4(AD), 5(AC).',
        'Take AB (1). Take CD (2). Take BC (3) — connects the {A,B} and {C,D} components. MST = {AB, CD, BC}, total 6.',
        'AD and AC would create cycles — rejected.',
      ] },
      { t: 'viz', id: 'graph-editor', props: { preset: 'mst' } },
      { t: 'cs', items: [
        { area: 'Network design', how: 'Laying cable between n cities at minimum total cost is the MST problem, literally.' },
        { area: 'Clustering', how: 'Single-linkage hierarchical clustering is MST pruning: cut the k heaviest edges of the MST to get k clusters.' },
        { area: 'Approximation', how: 'The MST is a 2-approximation for the Traveling Salesman (double the tree edges, take an Euler tour, shortcut) — a cornerstone of approximation algorithms.' },
      ] },
    ],
    practice: [
      { id: 'mst-p1', q: 'Kruskal’s algorithm on a graph with all edge weights distinct produces a unique MST. Why?', type: 'short', diff: 'medium',
        answer: 'With distinct weights, every cut has a unique lightest crossing edge, so the cut property forces the same edge choice at every step of Kruskal — the MST is unique.',
        explain: 'Uniqueness of edge weights ⇒ uniqueness of the MST (the cut property applies at every decision).',
      },
      { id: 'mst-p2', q: 'Is the lightest edge of a weighted graph always in every MST? What about the heaviest?', type: 'short', diff: 'medium',
        answer: 'The lightest edge: yes — it is lightest across every cut that separates its endpoints. The heaviest edge: no, unless it is a bridge (the only edge across some cut); a heaviest edge lying on a cycle is never in an MST (the cut property picks the lighter cycle edge instead… precisely: if e is heaviest on a cycle C, any spanning tree using e can swap in a lighter cycle edge — the exchange argument).',
        explain: 'Lightest is globally safe; heaviest is safe only when it is a bridge (no alternative exists).',
      },
    ],
  },
];

export const gtEulerHamilton: Concept[] = [
  {
    id: 'euler-hamilton',
    title: 'Euler & Hamilton Paths',
    domain: 'graph-theory',
    topic: true,
    summary: 'The two classic “tour” questions: cover every edge exactly once (Euler — solved in 1736 with a clean criterion) or visit every vertex exactly once (Hamilton — no simple criterion, NP-complete to decide).',
    level: 'advanced',
    csFields: ['theoretical-cs', 'competitive-programming', 'algorithms-dsa'],
    prerequisites: ['paths-walks-cycles', 'degree-and-handshake'],
    related: ['euler-paths', 'hamiltonian-paths', 'complexity-classes'],
    next: ['euler-paths'],
    content: [
      { t: 'intuition', text: 'Euler’s question (the Königsberg bridges: cross each of the 7 bridges exactly once?) was the birth of graph theory, and it has a beautiful complete answer: an Euler tour exists iff the odd-degree vertices are 0 or 2. Hamilton’s question (visit every city exactly once?) is the ancestor of the Traveling Salesman Problem and has no such criterion — deciding it is NP-complete. The contrast is the central drama of complexity theory in miniature: one “tour” problem yields to counting, the other to intractability.' },
      { t: 'cs', items: [
        { area: 'Routing & inspection', how: 'Mail-carrier and snow-plow routes are Euler tours (cover every street); if none exists, duplicate the cheapest edges to create one (the Route Inspection Problem).' },
        { area: 'Scheduling & TSP', how: 'Visiting every customer exactly once is Hamiltonian — the NP-complete core of logistics.' },
      ] },
    ],
    practice: [],
  },
  {
    id: 'euler-paths',
    title: 'Euler Paths & Circuits',
    domain: 'graph-theory',
    parent: 'euler-hamilton',
    summary: 'A trail using every edge exactly once. Criterion: 0 odd vertices → Euler circuit; 2 odd vertices → Euler path between them; more → none. Algorithm: Hierholzer, O(E).',
    level: 'advanced',
    csFields: ['competitive-programming', 'algorithms-dsa', 'theoretical-cs'],
    prerequisites: ['euler-hamilton', 'degree-and-handshake'],
    related: ['hamiltonian-paths', 'kruskal-prims'],
    content: [
      { t: 'thm', name: 'Euler’s theorem (1736)', statement: 'A connected undirected graph has (1) an Euler circuit iff every vertex has even degree; (2) an Euler path (not circuit) iff exactly two vertices have odd degree (the path starts and ends at them).',
        proofTitle: 'Proof (necessity; existence by induction)',
        proof: [
          'Necessity: in an Euler circuit, each visit to a vertex consumes one incoming and one outgoing edge — degrees even. In an Euler path, the start and end each have one unpaired edge — odd; internal visits pair up — even.',
          'Existence (0 odd case): induction on E. If the graph is a cycle, done. Otherwise, the even-degree graph has a cycle C; walk C, then for each vertex of C with remaining edges, the leftover subgraph has even degrees and a connected component touching C has an Euler circuit by induction (itself even-degree and connected when restricted) — splice it in at the touch point (Hierholzer’s splicing).',
          '2 odd case: add an artificial edge between the two odd vertices (making all degrees even), take the Euler circuit, and remove the artificial edge — the remainder is the Euler path between them.',
        ] },
      { t: 'ex', title: 'The Königsberg bridges', steps: [
        'Four landmasses, seven bridges. Degrees: 3, 3, 3, 3 — four odd vertices.',
        'The criterion says: no Euler trail exists. Euler’s 1736 answer to the city’s favorite puzzle: impossible.',
        'Add an eighth bridge to make the degrees even (e.g., connect two of the 3-degree banks): an Euler circuit now exists — the city could have solved its own tourism problem with a bridge.',
      ] },
      { t: 'ex', title: 'Hierholzer in practice', steps: [
        'Start anywhere (all degrees even); follow unused edges until you return to the start — a closed tour T.',
        'If T missed some edge, some vertex v of T has unused edges left; the leftover subgraph around v has even degrees, so it contains a closed tour from v.',
        'Splice that tour into T at v. Repeat until no edge is unused. O(E) total.',
      ] },
      { t: 'cs', items: [
        { area: 'Street sweeping', how: 'Route inspection: find the minimum-cost set of edges to duplicate so an Euler tour covers all streets — a matching problem on the odd vertices.' },
        { area: 'Circuit PCB', how: 'Designing a test path that traverses every trace once is an Euler tour problem in the trace graph.' },
      ] },
    ],
    practice: [
      { id: 'ep-p1', q: 'A connected graph has degree sequence 2, 2, 3, 3, 4. Does it have an Euler path? A circuit?', type: 'short', diff: 'easy',
        answer: 'Two odd-degree vertices (the two 3s): an Euler path exists, from one odd vertex to the other; no Euler circuit (not all degrees even).',
        explain: 'Count the odd degrees: 0 → circuit, 2 → path, else → none.',
      },
      { id: 'ep-p2', q: 'Why must an Euler circuit start and end at the same vertex, and why are all degrees even in its graph?', type: 'short', diff: 'medium',
        answer: 'A circuit is a closed trail: every time the trail enters a vertex it leaves it, so incident edges pair up at every vertex — even degrees, and the start has no unpaired edge (it is also the end).',
        explain: 'The pairing argument is the whole necessity proof.',
      },
    ],
  },
  {
    id: 'hamiltonian-paths',
    title: 'Hamiltonian Paths & Cycles',
    domain: 'graph-theory',
    parent: 'euler-hamilton',
    summary: 'A path visiting every vertex exactly once. No simple characterization exists — Dirac and Ore give sufficient conditions, and deciding existence is NP-complete.',
    level: 'advanced',
    csFields: ['theoretical-cs', 'competitive-programming', 'algorithms-dsa'],
    prerequisites: ['euler-hamilton'],
    related: ['complexity-classes', 'turing-machines'],
    content: [
      { t: 'def', title: 'Hamiltonian', text: 'A **Hamiltonian path** visits every vertex exactly once; a **Hamiltonian cycle** is a closed one. Deciding whether a graph has one is NP-complete (it generalizes 3-SAT via Karp’s reductions).' },
      { t: 'thm', name: 'Sufficient conditions (Dirac, Ore)', statement: 'Dirac: if every vertex has degree ≥ n/2 (n ≥ 3), G is Hamiltonian. Ore: if for every non-adjacent pair u, v, deg(u) + deg(v) ≥ n, G is Hamiltonian.',
        proof: [
          'Dirac follows from Ore (non-adjacent u, v have deg(u)+deg(v) ≥ n).',
          'Ore’s proof (sketch): take a longest path P. Its endpoints’ neighbors all lie on P, else P extends. If no endpoint’s neighbor set “crosses” the other endpoint’s, sum the degrees — each neighbor of u pairs with a non-neighbor of v on P — and the Ore bound forces a shortcut that closes a cycle containing all of P; then the degree bound forces the cycle to use all vertices.',
        ] },
      { t: 'ex', title: 'Why these are sufficient, not necessary', steps: [
        'A cycle Cₙ with a few extra chords removed can stay Hamiltonian while violating deg ≥ n/2 (a pure n-cycle has degree 2 < n/2 for n > 4 — yet is trivially Hamiltonian).',
        'The conditions are tools: they certify Hamiltonicity quickly when they hold; when they fail, nothing is decided.',
      ] },
      { t: 'cs', items: [
        { area: 'TSP', how: 'The traveling salesman problem asks for the minimum-weight Hamiltonian cycle — NP-hard to optimize, and its decision form is the NP-complete problem that anchors the class.' },
        { area: 'VLSI', how: 'Pin ordering and probe-path design reduce to Hamiltonian-like constraints on chip graphs.' },
        { area: 'Puzzles', how: 'Knight’s tour on a chessboard: a Hamiltonian cycle in the knight-move graph (solved by Warnsdorff’s heuristic — a greedy walk that works on the standard board).' },
      ] },
    ],
    practice: [
      { id: 'hp-p1', q: 'Does the complete graph Kₙ have a Hamiltonian cycle? For which n?', type: 'short', diff: 'easy',
        answer: 'Yes, for all n ≥ 3: list the vertices in any order and close the loop — every edge exists.',
        explain: 'Completeness makes every ordering a valid Hamiltonian cycle.',
      },
      { id: 'hp-p2', q: 'Why is “exactly one Hamiltonian cycle” a red flag for the condition “deg(u)+deg(v) ≥ n for non-adjacent u, v”? (Explain why Ore’s condition, if it holds, tends to produce many cycles.)', type: 'short', diff: 'hard',
        answer: 'Under Ore’s bound, for any edge uv of a longest-path cycle, the crossing-neighbor argument gives shortcuts that produce many distinct cycles; in fact, graphs satisfying Ore are not just Hamiltonian but 2-connected with abundant cycles — a graph with exactly one Hamiltonian cycle has thin structure that typically violates the degree sum (its “critical” vertices have small degrees). The point of the exercise: sufficient conditions certify abundance, not uniqueness.',
        explain: 'Ore/Dirac conditions are one-way certificates; they say nothing when they fail and imply rich cycle structure when they hold.',
      },
    ],
  },
];

export const gtMore: Concept[] = [
  {
    id: 'bipartite-graphs',
    title: 'Bipartite Graphs',
    domain: 'graph-theory',
    parent: undefined,
    summary: 'Vertex sets split into two sides with edges only across: K_{m,n}, matching, and the theorem that characterizes them — a graph is bipartite iff it has no odd cycle.',
    level: 'core',
    csFields: ['algorithms-dsa', 'theoretical-cs', 'data-science'],
    prerequisites: ['graph-types'],
    related: ['graph-coloring', 'matching', 'markov-chains'],
    content: [
      { t: 'thm', name: 'Bipartite characterization', statement: 'G is bipartite iff it contains no odd cycle.',
        proofTitle: 'Proof',
        proof: [
          'If G is bipartite with parts X, Y: any cycle alternates X, Y, X, Y, … and closes only after an even number of steps — no odd cycle.',
          'Conversely, if G has no odd cycle: 2-color each component by BFS (color by distance parity from the root). A bad edge (same-colored endpoints) lies on a walk from the root; its endpoints’ depths have the same parity, so the closed walk formed has odd length — containing an odd cycle, contradiction. So the 2-coloring is valid: G is bipartite.',
        ] },
      { t: 'props', items: [
        { title: 'Detection', text: 'BFS 2-coloring detects bipartiteness in O(V+E) — the test is a byproduct of any traversal.' },
        { title: 'Complete bipartite', text: '$K_{m,n}$ has all m·n cross-edges; its independence number is max(m, n), and its chromatic number is 2.' },
        { title: 'Matchings', text: 'A **matching** is a set of edges with no shared endpoints. Hall’s theorem: a bipartite graph has a matching covering X iff every subset S ⊆ X has |N(S)| ≥ |S|.' },
      ] },
      { t: 'ex', title: 'Bipartite structures in CS', steps: [
        'Job assignment: applicants and jobs form a bipartite graph; a matching covers the applicants (Hall’s condition is exactly “enough distinct jobs for every group”).',
        'Sudoku: candidates and cells in a row/box are bipartite; exact cover reduces to matching in a bipartite hypergraph.',
        'Social graphs that are strictly “user ↔ post” are bipartite by construction (the “bipartite user-item” model of recommender systems).',
      ] },
      { t: 'cs', items: [
        { area: 'Assignment problems', how: 'The Hungarian algorithm solves weighted bipartite matching in O(n³) — the model for scheduling workers to machines.' },
        { area: '2-colorability', how: '“Can this graph be 2-colored?” is exactly “is it bipartite?” — decided by BFS in linear time.' },
      ] },
    ],
    practice: [
      { id: 'bg-p1', q: 'Is the cycle C₅ bipartite? C₆?', type: 'short', diff: 'easy',
        answer: 'C₅: no (odd cycle). C₆: yes (even cycle — 2-color by alternating around the ring).',
        explain: 'The no-odd-cycle criterion decides both in one line.',
      },
      { id: 'bg-p2', q: 'A bipartite graph has parts of size 4 and 6. What is the maximum number of edges it can have?', type: 'numeric', diff: 'easy', answer: '24',
        explain: 'K_{4,6} has 4·6 = 24 edges — the complete bipartite graph maximizes the count.',
      },
    ],
  },
  {
    id: 'graph-coloring',
    title: 'Graph Coloring',
    domain: 'graph-theory',
    parent: undefined,
    summary: 'Assigning colors to vertices so no adjacent pair shares one: the chromatic number, greedy bounds, planar graphs (five colors, four on the plane), and the algorithmic uses — scheduling, register allocation, frequency planning.',
    level: 'advanced',
    csFields: ['algorithms-dsa', 'theoretical-cs', 'competitive-programming', 'operating-systems'],
    prerequisites: ['bipartite-graphs'],
    related: ['bipartite-graphs', 'planarity', 'complexity-classes'],
    content: [
      { t: 'def', title: 'Graph coloring', text: 'A **proper k-coloring** assigns one of k colors to each vertex so adjacent vertices differ. The **chromatic number** χ(G) is the smallest k. Two vertices may share a color iff they are non-adjacent — a coloring is a partition into independent sets.' },
      { t: 'props', title: 'Bounds and theorems', items: [
        { title: 'Greedy bound', text: 'Any vertex ordering gives a valid greedy coloring with ≤ Δ+1 colors (a vertex has at most Δ colored neighbors). Brouwer–… no: the greedy bound is Δ + 1; Brooks’ theorem sharpens it: χ(G) ≤ Δ unless G is a complete graph or an odd cycle, where χ = n or 3.' },
        { title: 'Planar graphs', text: 'The Four Color Theorem: every planar graph is 4-colorable (proved 1976, computer-assisted; the 5-color theorem has a short proof). Planar ⇒ χ ≤ 4.' },
        { title: 'Hardness', text: 'Deciding χ(G) ≤ 3 is NP-complete — 2-coloring (bipartiteness) is linear-time, 3-coloring is already intractable. This step-function is a canonical complexity example.' },
      ] },
      { t: 'viz', id: 'graph-editor', props: { preset: 'coloring' } },
      { t: 'ex', title: 'Register allocation as coloring', steps: [
        'Build the interference graph: a variable is a vertex; an edge joins two variables live at the same program point.',
        'Coloring with R colors assigns the R physical registers so no interfering pair shares one.',
        'If χ > R, some variable must spill to memory — the compiler’s cost model picks which (heuristically, via the “simplifiable kernel” of the graph).',
      ], result: 'A compiler’s register allocator is a graph-coloring algorithm with a cost function — theory in production silicon.' },
      { t: 'cs', items: [
        { area: 'Scheduling', how: 'Course timetabling: courses are vertices, conflicts are edges, colors are time slots. χ is the minimum number of slots.' },
        { area: 'Frequency planning', how: 'Cell towers with incompatible frequencies are adjacent in the interference graph — coloring assigns frequencies legally.' },
        { area: 'Map coloring', how: 'The original problem: four colors suffice for any map (adjacent regions = adjacent vertices of the dual planar graph).' },
      ] },
    ],
    practice: [
      { id: 'gc-p1', q: 'What is the chromatic number of a tree? Of an even cycle? Of K₅?', type: 'short', diff: 'easy',
        answer: 'Tree: 2 (bipartite). Even cycle: 2. K₅: 5 (every pair adjacent — all vertices need distinct colors).',
        explain: 'Bipartite ⇒ 2; complete ⇒ n; odd cycle ⇒ 3.',
      },
      { id: 'gc-p2', q: 'Give a 4-vertex graph with chromatic number 3, and argue it needs at least 3.', type: 'short', diff: 'medium',
        answer: 'A triangle plus a leaf attached to one triangle vertex (a “paw”). The triangle forces 3 distinct colors; the leaf takes any color except its neighbor’s — 3 colors suffice and are necessary.',
        explain: 'A K₃ subgraph forces χ ≥ 3; the extra vertex adds no new requirement.',
      },
    ],
  },
  {
    id: 'planarity',
    title: 'Planarity',
    domain: 'graph-theory',
    parent: undefined,
    summary: 'When can a graph be drawn in the plane without crossings? Euler’s formula V − E + F = 2, the 3V−6 bound, and Kuratowski’s theorem (K₅ and K₃,₃ as the forbidden minors).',
    level: 'advanced',
    csFields: ['theoretical-cs', 'computer-graphics', 'algorithms-dsa'],
    prerequisites: ['graph-types'],
    related: ['graph-coloring', 'degree-and-handshake'],
    content: [
      { t: 'thm', name: 'Euler’s formula (planar graphs)', statement: 'For a connected planar graph with V vertices, E edges, and F faces: $V - E + F = 2$.',
        proofTitle: 'Proof (induction on E)',
        proof: [
          'Base: a single vertex, V = 1, E = 0, F = 1 (the outer face): 1 − 0 + 1 = 2.',
          'Step: if the graph has a bridge, remove it — E drops by 1, and two faces merge into one (F drops by 1), V unchanged; V − E + F is invariant, so the formula holds by induction.',
          'If no bridge remains relevant… if the graph has a cycle edge, removing an edge on a cycle merges the two faces it bounded: E − 1, F − 1, V unchanged — again invariant. By induction the smaller graph satisfies the formula, so does the original.',
        ] },
      { t: 'props', items: [
        { title: 'Edge bound', text: 'For planar G with V ≥ 3: every face is bounded by ≥ 3 edges, and each edge borders ≤ 2 faces, so 2E ≥ 3F. With Euler: E ≤ 3V − 6. For bipartite planar: E ≤ 2V − 4 (faces have ≥ 4 sides).' },
        { title: 'Kuratowski’s theorem', text: 'G is planar iff it contains no subgraph that is a subdivision of K₅ or K₃,₃. These two are the minimal obstructions — the graph-theoretic “primes” of non-planarity.' },
        { title: 'Testing', text: 'Planarity is decidable in linear time (Hopcroft–Tarjan 1971) — a landmark algorithm; the practical test is used in circuit layout and map rendering.' },
      ] },
      { t: 'ex', title: 'Using the bound', steps: [
        'K₅ has V = 5, E = 10 > 3·5 − 6 = 9: not planar (by the bound).',
        'K₃,₃ has V = 6, E = 9 > 2·6 − 4 = 8: not planar (the bipartite bound — the 3V−6 bound allows 12, so the stronger bipartite version is needed here).',
        'A network with 100 routers and 280 links: 280 ≤ 3·100 − 6 = 294 — the bound does not rule out planarity (the bound is necessary, not sufficient).',
      ] },
      { t: 'cs', items: [
        { area: 'Circuit layout', how: 'PCB routing on a single layer requires planarity of the connection graph; non-planar designs need multiple layers (equivalently, a book embedding).' },
        { area: 'Geometric algorithms', how: 'Planar graphs underlie triangulations, Voronoi diagrams, and sparse geometric networks — the O(V) structure that makes many geometry algorithms near-linear.' },
      ] },
    ],
    practice: [
      { id: 'plan-p1', q: 'A connected planar graph has 20 vertices and 45 edges. How many faces does it have?', type: 'numeric', diff: 'easy', answer: '27',
        explain: 'Euler: F = 2 − V + E = 2 − 20 + 45 = 27.',
      },
      { id: 'plan-p2', q: 'Why does K₃,₃ fail the 3V−6 test but still need the bipartite bound to prove non-planarity?', type: 'short', diff: 'medium',
        answer: 'K₃,₃ has V = 6, E = 9. The general bound gives E ≤ 3·6 − 6 = 12 — 9 ≤ 12 passes, so the general bound cannot exclude it. The bipartite version (all faces have ≥ 4 edges) gives E ≤ 2·6 − 4 = 8, and 9 > 8: non-planar. The stronger hypothesis (bipartite) buys the stronger bound.',
        explain: 'Planarity bounds are a family; use the strongest one the graph’s structure supports.',
      },
    ],
  },
];

export const graphTheory1: Concept[] = [
  ...gtBasics, ...gtPaths, ...gtTrees, ...gtEulerHamilton, ...gtMore,
];
