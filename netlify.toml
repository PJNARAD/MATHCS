[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA — deep links (e.g. /concept/rsa) must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
