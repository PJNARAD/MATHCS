import type { Concept } from '../types';

// ---------------------------------------------------------------------------
// NUMBER THEORY — 2: GCD & LCM, Modular Arithmetic, Great Theorems, RSA
// ---------------------------------------------------------------------------

export const ntGcdLcm: Concept[] = [
  {
    id: 'gcd-lcm',
    title: 'GCD and LCM',
    domain: 'number-theory',
    topic: true,
    summary: 'The greatest common divisor and least common multiple — the two canonical “shared” quantities of a pair of integers, computable in logarithmic time by the Euclidean algorithm.',
    level: 'core',
    csFields: ['competitive-programming', 'cryptography', 'algorithms-dsa'],
    prerequisites: ['divisibility-core'],
    related: ['gcd', 'lcm', 'euclidean-algorithm', 'bezout-identity'],
    next: ['gcd'],
    content: [
      { t: 'intuition', text: 'The gcd(a, b) is the largest integer that divides both a and b — the “common structure” they share. The lcm(a, b) is the smallest positive integer both divide — the “first moment” they synchronize. One goes down (factors shared), the other goes up (multiples shared), and they are locked together by the identity gcd(a,b)·lcm(a,b) = |ab|.' },
      { t: 'cs', items: [
        { area: 'Fraction reduction', how: 'a/b in lowest terms divides numerator and denominator by gcd(a, b) — the single most common gcd use in numerics.' },
        { area: 'Gear trains', how: 'Gears with a and b teeth mesh periodically; their cycle length is lcm(a, b) rotations… precisely: the train repeats every lcm(a,b)/gcd… the ratio of angular speeds is a/b, and periodicity is an lcm question.' },
        { area: 'Music & sampling', how: 'Two signals of periods p and q realign every lcm(p, q) samples; aliasing analysis is lcm arithmetic.' },
      ] },
    ],
    practice: [],
  },
  {
    id: 'gcd',
    title: 'Greatest Common Divisor',
    domain: 'number-theory',
    parent: 'gcd-lcm',
    summary: 'gcd(a, b): the largest integer dividing both. Properties, the gcd·lcm identity, and the characterization as the smallest positive linear combination.',
    level: 'core',
    csFields: ['competitive-programming', 'cryptography', 'algorithms-dsa'],
    prerequisites: ['divisibility-core'],
    related: ['euclidean-algorithm', 'bezout-identity', 'lcm'],
    next: ['euclidean-algorithm'],
    content: [
      { t: 'def', title: 'GCD', text: 'For integers a, b not both zero, $\\gcd(a, b)$ is the largest positive integer dividing both a and b. It is well-defined by the FTA: take the minimum exponent of each common prime.' },
      { t: 'props', title: 'Properties', items: [
        { title: 'Symmetry & sign', text: '$\\gcd(a,b) = \\gcd(b,a) = \\gcd(|a|,|b|)$.' },
        { title: 'The subtraction rule', text: '$\\gcd(a, b) = \\gcd(b, a - b)$ — the engine of the Euclidean algorithm. More generally $\\gcd(a, b) = \\gcd(b, a \\bmod b)$.' },
        { title: 'Linear combination', text: '$\\gcd(a,b)$ is the smallest positive value of $xa + yb$ over integers x, y (Bézout).' },
        { title: 'Divisor characterization', text: '$d \\mid a$ and $d \\mid b$ iff $d \\mid \\gcd(a,b)$.' },
        { title: 'Coprime', text: '$\\gcd(a,b) = 1$ means a, b are coprime — they share no prime factor.' },
        { title: 'The product identity', text: '$\\gcd(a,b) \\cdot \\mathrm{lcm}(a,b) = |ab|$ for nonzero a, b.' },
      ] },
      { t: 'ex', title: 'gcd by prime factorization', steps: [
        'a = 84 = 2²·3·7; b = 144 = 2⁴·3².',
        'Common primes with minimum exponents: 2²·3 = 12.',
        'gcd(84, 144) = 12; lcm = 2⁴·3²·7 = 1008.',
        'Check: 12 · 1008 = 12096 = 84 · 144. ✓',
      ] },
      { t: 'ex', title: 'Coprimality in practice', steps: [
        'Reducing 144/192: gcd(144,192) = 48, so 144/192 = 3/4.',
        'Why gcd(192, 48): 192 = 48·4, so 48 | 192; 144 = 48·3. The shared factor is exactly 48.',
      ], result: 'Reduction is gcd arithmetic; it is what makes fractions canonical.' },
      { t: 'cs', items: [
        { area: 'RSA key gen', how: 'The public exponent e must satisfy gcd(e, φ(n)) = 1 — a gcd test gates the whole key.' },
        { area: 'Geometry', how: 'Grid points visible from the origin: (a, b) is visible iff gcd(a, b) = 1; counting them gives the 6/π² density of coprime pairs.' },
      ] },
    ],
    practice: [
      { id: 'gcd-p1', q: 'Compute gcd(1071, 462) by hand (show the steps).', type: 'short', diff: 'easy',
        answer: '1071 = 2·462 + 147; 462 = 3·147 + 21; 147 = 7·21 + 0. gcd = 21.',
        explain: 'Each step replaces the pair by (b, remainder); the last nonzero remainder is the gcd.',
      },
      { id: 'gcd-p2', q: 'If gcd(a, b) = 1 and a | bc, must a | c? Prove it.', type: 'proof', diff: 'medium',
        answer: 'Yes. Bézout: xa + yb = 1. Multiply by c: xac + ybc = c. a | xac and a | ybc (since a | bc), so a | c.',
        explain: 'This is Euclid’s lemma generalized — the gcd = 1 hypothesis is used exactly once, in Bézout.',
      },
    ],
  },
  {
    id: 'lcm',
    title: 'Least Common Multiple',
    domain: 'number-theory',
    parent: 'gcd-lcm',
    summary: 'lcm(a, b): the smallest positive integer both a and b divide. Computed from prime exponents (the maximums) or from the identity lcm = |ab|/gcd.',
    level: 'core',
    csFields: ['competitive-programming', 'algorithms-dsa', 'operating-systems'],
    prerequisites: ['gcd'],
    related: ['gcd', 'gcd-lcm'],
    next: ['euclidean-algorithm'],
    content: [
      { t: 'def', title: 'LCM', text: '$\\mathrm{lcm}(a, b)$ is the smallest positive integer divisible by both a and b. From prime factorizations, take the **maximum** exponent of each prime (the gcd takes the minimum).' },
      { t: 'formula', name: 'LCM from GCD', latex: '\\mathrm{lcm}(a, b) = \\frac{|ab|}{\\gcd(a, b)}', note: 'Derivation: if a = ∏p^{αp}, b = ∏p^{βp}, then lcm = ∏p^{max(α,β)} and ab/gcd = ∏p^{α+β−min(α,β)} = ∏p^{max(α,β)}.' },
      { t: 'ex', title: 'Synchronizing schedules', steps: [
        'Bus A arrives every 12 minutes; bus B every 18 minutes; both stop at 08:00.',
        'They next stop together after lcm(12, 18) = 36 minutes (08:36).',
        'gcd(12, 18) = 6 is the “beat” of their misalignment; 12·18/6 = 36.',
      ], result: '“When will they line up again?” is the lcm question — ubiquitous in scheduling and signal processing.' },
      { t: 'cs', items: [
        { area: 'Timers & scheduling', how: 'Two periodic tasks with periods p, q fire together every lcm(p, q) ticks — overflow analysis of periodic systems is lcm arithmetic.' },
        { area: 'FPGA clocking', how: 'Clock domain crossing analysis: events from domains of frequency f₁, f₂ align every lcm of their periods.' },
      ] },
    ],
    practice: [
      { id: 'lcm-p1', q: 'Two cyclists lap a track in 8 and 12 minutes. Starting together, after how many minutes do they next meet at the start line?', type: 'numeric', diff: 'easy', answer: '24',
        explain: 'lcm(8, 12) = 24 (8·12/4).',
      },
      { id: 'lcm-p2', q: 'Prove: gcd(a,b)·lcm(a,b) = ab for positive a, b.', type: 'proof', diff: 'medium',
        answer: 'Let a = ∏ p^{αp}, b = ∏ p^{βp}. gcd = ∏ p^{min(α,β)}, lcm = ∏ p^{max(α,β)}. Product of exponents: min + max = α + β = exponent in ab. QED.',
        explain: 'The identity is a statement about exponents: min + max = sum.',
      },
    ],
  },
  {
    id: 'euclidean-algorithm',
    title: 'The Euclidean Algorithm',
    domain: 'number-theory',
    parent: 'gcd-lcm',
    summary: 'The 2,300-year-old algorithm for gcd: repeatedly replace (a, b) with (b, a mod b) until b = 0. O(log min(a,b)) steps — the fastest classical gcd.',
    level: 'core',
    csFields: ['competitive-programming', 'cryptography', 'algorithms-dsa', 'number-theory'],
    prerequisites: ['gcd', 'division-algorithm'],
    related: ['bezout-identity', 'modular-inverses'],
    next: ['bezout-identity'],
    content: [
      { t: 'thm', name: 'Correctness', statement: 'The Euclidean algorithm terminates with gcd(a, b), where the loop is: while b ≠ 0: (a, b) ← (b, a mod b).',
        proofTitle: 'Proof',
        proof: [
          'Invariant: gcd(a, b) is unchanged by the step. Indeed gcd(a, b) = gcd(b, a mod b): if a = bq + r, any common divisor of a, b divides r = a − bq, and any common divisor of b, r divides a = bq + r. Same divisor set, same maximum.',
          'Termination: the second component strictly decreases (0 ≤ a mod b < b) and is a nonnegative integer, so it reaches 0 in finitely many steps (well-ordering).',
          'At b = 0, gcd(a, 0) = a — the algorithm’s answer, equal to the original gcd by the invariant.',
        ] },
      { t: 'ex', title: 'Full run: gcd(1071, 462)', steps: [
        '1071 = 2 · 462 + 147 → pair (462, 147).',
        '462 = 3 · 147 + 21 → pair (147, 21).',
        '147 = 7 · 21 + 0 → pair (21, 0).',
        'Stop: gcd = 21.',
      ], result: 'Three divisions. The remainders shrink: 462 → 147 → 21 → 0.' },
      { t: 'thm', name: 'Running time', statement: 'The Euclidean algorithm takes at most $5 \\log_{10}(\\min(a,b)) + 1$ divisions — the worst case is consecutive Fibonacci numbers (Fibonacci growth is the slowest possible shrinkage, ratio φ per step).',
        proof: [
          'Each step satisfies b_{i+1} ≤ b_i / φ is false in general, but the Fibonacci argument: if b_i < F_{k+2}, then at most k steps remain. The slowest shrink is exactly the Fibonacci recurrence, and F_k ~ φᵏ/√5, giving logarithmic step count.',
        ] },
      { t: 'viz', id: 'euclid', props: { a: 1071, b: 462 } },
      { t: 'analogy', title: 'Think of it like this', text: 'You have two ropes of lengths a and b. The gcd is the longest measuring stick that measures both exactly. The Euclidean algorithm finds it by laying the shorter rope along the longer: whatever sticks out becomes the new shorter rope. Repeat — the stick that fits with no remainder is the answer.' },
      { t: 'cs', items: [
        { area: 'Built-in everywhere', how: 'Every standard library has a gcd (C++17 std::gcd, Python math.gcd) — the Euclidean algorithm, sometimes with binary (Stein) optimization for machines without fast division.' },
        { area: 'Cryptography', how: 'Computing d = e⁻¹ mod φ(n) is an extended Euclidean run — the heart of RSA key generation.' },
      ] },
    ],
    practice: [
      { id: 'ea-p1', q: 'Run the Euclidean algorithm on (252, 105).', type: 'short', diff: 'easy',
        answer: '252 = 2·105 + 42; 105 = 2·42 + 21; 42 = 2·21 + 0. gcd = 21.',
        explain: 'Quotients all 2 — a Fibonacci-like worst-case pattern.',
      },
      { id: 'ea-p2', q: 'Why do consecutive Fibonacci numbers F(n), F(n+1) force the maximum number of steps?', type: 'short', diff: 'hard',
        answer: 'Each step (F(k+1), F(k)) → (F(k), F(k−1)) subtracts one index — the slowest possible decrease since F(k) − F(k−1) = F(k−2) < F(k)/2 is the weakest shrink ratio φ⁻¹ ≈ 0.618. Any other pair shrinks faster on average, so Fibonacci pairs maximize the step count for a given size.',
        explain: 'The Fibonacci sequence is the slowest-growing integer sequence with integer steps — duality of slowest growth and slowest shrink.',
      },
    ],
  },
  {
    id: 'bezout-identity',
    title: "Bézout's Identity & the Extended Euclidean Algorithm",
    domain: 'number-theory',
    parent: 'gcd-lcm',
    summary: 'The gcd is a linear combination: there exist x, y with ax + by = gcd(a, b). The extended Euclidean algorithm finds them — and thereby computes modular inverses.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['euclidean-algorithm'],
    related: ['modular-inverses', 'rsa-cryptography', 'linear-congruences'],
    content: [
      { t: 'thm', name: "Bézout's identity", statement: 'For integers a, b not both zero, there exist integers x, y with $ax + by = \\gcd(a, b)$. The gcd is the smallest positive such linear combination.',
        proofTitle: 'Proof (sketch)',
        proof: [
          'Existence: the set S = {xa + yb > 0 : x, y ∈ ℤ} is nonempty and has a least element d = xa + yb by well-ordering.',
          'Show d | a: write a = dq + r, 0 ≤ r < d. Then r = a − dq = a − q(xa + yb) = (1 − qx)a + (−qy)b is a smaller positive element of S unless r = 0. So r = 0 and d | a; similarly d | b.',
          'So d is a common divisor, hence d ≤ gcd(a, b); but any common divisor divides every xa + yb, so gcd(a, b) ≤ d. Thus d = gcd(a, b), with the witness (x, y) in hand.',
        ] },
      { t: 'ex', title: 'Finding x, y for gcd(240, 46) = 2', steps: [
        '240 = 5·46 + 10 → 10 = 240 − 5·46.',
        '46 = 4·10 + 6 → 6 = 46 − 4·10 = 46 − 4(240 − 5·46) = 24·46 − 4·240.',
        '10 = 1·10 + 0·6… continue: 10 = 1·6 + 4 → 4 = 10 − 6 = (240 − 5·46) − (24·46 − 4·240) = 5·240 − 29·46.',
        '6 = 1·4 + 2 → 2 = 6 − 4. Back-substituting the earlier expressions: 2 = −9·240 + 47·46.',
        'Check: −9·240 + 47·46 = −2160 + 2162 = 2. ✓ (The mechanical extended Euclid tracks these coefficients automatically; hand back-substitution is where slips creep in.)'], result: 'The mechanical extended Euclid is the reliable way; hand back-substitution is where slips creep in.' },
      { t: 'callout', kind: 'insight', text: 'Bézout is the bridge from “divisibility” to “invertibility”: gcd(a, n) = 1 means ax + ny = 1, and reducing mod n gives a·x ≡ 1 (mod n) — x is the modular inverse of a. The whole of “division modulo n” is Bézout wearing a different hat.' },
      { t: 'cs', items: [
        { area: 'RSA', how: 'd = e⁻¹ mod φ(n) is the x from Bézout for (e, φ(n)).' },
        { area: 'Diophantine equations', how: 'ax + by = c has integer solutions iff gcd(a, b) | c — Bézout scaled by c/gcd.' },
        { area: 'Clock synchronization', how: 'Two periodic events of periods a, b can hit the same instant iff the offset is a linear combination — a Bézout condition.' },
      ] },
    ],
    practice: [
      { id: 'bez-p1', q: 'Find x, y with 35x + 15y = 5.', type: 'short', diff: 'medium',
        answer: '35 = 2·15 + 5, so 5 = 35 − 2·15: x = 1, y = −2.',
        explain: 'The first Euclidean step already gives the combination when the remainder is the gcd.',
      },
      { id: 'bez-p2', q: 'Does 7x + 12y = 1 have integer solutions? What about = 2?', type: 'short', diff: 'easy',
        answer: '= 1: yes (gcd(7,12)=1): 7·7 + 12·(−4) = 49 − 48 = 1. = 2: yes, double it: x = 14, y = −8.',
        explain: 'Solvability iff gcd | c; when gcd = 1 every c works.',
      },
    ],
  },
];

export const ntModular: Concept[] = [
  {
    id: 'modular-arithmetic',
    title: 'Modular Arithmetic',
    domain: 'number-theory',
    topic: true,
    summary: 'Arithmetic on a clock: congruence, residue classes, and the operations that respect them. The algebra of “what remains” — the language of clocks, checksums, and cryptography.',
    level: 'core',
    csFields: ['cryptography', 'competitive-programming', 'computer-architecture', 'number-theory'],
    prerequisites: ['division-algorithm'],
    related: ['congruence', 'residue-classes', 'modular-arithmetic-ops', 'modular-inverses'],
    next: ['congruence'],
    content: [
      { t: 'intuition', text: 'Modular arithmetic keeps only the remainder after division by n. On a 12-hour clock, 9 + 7 is not 16 — it is 4. In this arithmetic, 16 and 4 are the same number (congruent mod 12), and all the familiar operations work — with one major exception: division does not always exist, and that restriction is exactly where number theory gets interesting (and where cryptography hides its hardness).' },
      { t: 'cs', items: [
        { area: 'Overflow', how: 'Unsigned wraparound IS modular arithmetic: uint32 overflow computes mod 2³² automatically.' },
        { area: 'Checksums', how: 'Luhn, ISBN-10, and IP checksums are modular sums — designed so that one-digit errors change the sum.' },
        { area: 'Cryptography', how: 'RSA, Diffie–Hellman, and AES’s Galois field all live in modular/finite-field arithmetic.' },
      ] },
    ],
    practice: [],
  },
  {
    id: 'congruence',
    title: 'Congruence (a ≡ b mod n)',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'a ≡ b (mod n) means n | (a − b). Congruence is an equivalence relation compatible with +, −, × — and with exponentiation. The foundational relation of modular arithmetic.',
    level: 'core',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['modular-arithmetic', 'divisibility-core'],
    related: ['residue-classes', 'modular-arithmetic-ops', 'fermats-little-theorem'],
    next: ['residue-classes'],
    content: [
      { t: 'def', title: 'Congruence', text: 'For n ≥ 1, integers a, b: $a \\equiv b \\pmod n$ reads “a is congruent to b modulo n” and means $n \\mid (a - b)$. Equivalently, a and b have the same remainder upon division by n.' },
      { t: 'thm', name: 'Congruence is an equivalence relation', statement: '$\\equiv \\pmod n$ is reflexive, symmetric, and transitive.',
        proofTitle: 'Proof',
        proof: [
          'Reflexive: n | (a − a) = 0.',
          'Symmetric: n | (a−b) ⇒ n | −(a−b) = (b−a).',
          'Transitive: n | (a−b) and n | (b−c) ⇒ n | [(a−b) + (b−c)] = (a−c).',
        ] },
      { t: 'props', title: 'Compatibility with operations', items: [
        { title: 'Addition & subtraction', text: 'a ≡ b and c ≡ d (mod n) ⇒ a + c ≡ b + d and a − c ≡ b − d (mod n). (Divisibility is preserved by linear combinations.)' },
        { title: 'Multiplication', text: 'a ≡ b (mod n) ⇒ a·c ≡ b·c (mod n) for any c. Also: if a ≡ b and c ≡ d then ac ≡ bd.' },
        { title: 'Powers', text: 'a ≡ b (mod n) ⇒ aᵏ ≡ bᵏ (mod n) for k ≥ 0 — repeat multiplication.' },
        { title: 'Division — the catch', text: 'a ≡ b (mod n) does NOT let you divide by c: 2·3 ≡ 1·3 (mod 9) but 2 ≢ 1. You may divide by c only when gcd(c, n) = 1 (then use c’s inverse), or reduce the modulus by gcd(c, n).' },
      ] },
      { t: 'ex', title: 'Congruences in practice', steps: [
        'Compute 7⁵ mod 5: 7 ≡ 2 (mod 5), so 7⁵ ≡ 2⁵ = 32 ≡ 2 (mod 5).',
        'Compute 123456789 mod 9: digit sum = 45 ≡ 0, so the number ≡ 0 (mod 9) — divisible by 9.',
        'Prove n² − n is even: n² ≡ n (mod 2) since both parities agree… n ≡ 0 or 1 (mod 2); in both cases n² ≡ n, so n² − n ≡ 0 (mod 2).',
      ] },
      { t: 'viz', id: 'modular-clock', props: { modulus: 12 } },
      { t: 'analogy', title: 'Think of it like this', text: 'A clock forgets everything except the hand position: 3:00 + 9 hours = 12:00, not “12 o’clock in the future”. Congruence is the clock’s memory rule — two times are the same if the clock cannot tell them apart.' },
      { t: 'cs', items: [
        { area: 'Cyclic buffers', how: 'An index into a buffer of size n is an element of ℤ/nℤ; wrapping `(i+1) % n` is congruent increment.' },
        { area: 'Round-robin', how: 'Assigning task k to worker k mod m is a congruence class assignment — balanced by construction.' },
      ] },
    ],
    practice: [
      { id: 'cong-p1', q: 'Compute 2¹⁰ mod 11.', type: 'numeric', diff: 'easy', answer: '1',
        explain: '2¹⁰ = 1024 = 93·11 + 1. (Also Fermat: 2¹⁰ ≡ 1 mod 11 since 11 is prime.)',
      },
      { id: 'cong-p2', q: 'Why can’t we “divide both sides” of 6x ≡ 3 (mod 9) by 3 to get 2x ≡ 1? Solve it correctly.', type: 'short', diff: 'medium',
        answer: 'gcd(3, 9) = 3 ≠ 1, so division by 3 is not valid as-is. Divide the congruence and the modulus by 3: 2x ≡ 1 (mod 3). Now 2⁻¹ ≡ 2 (mod 3), so x ≡ 2 (mod 3): x ∈ {…, 2, 5, 8, 11, …}. Indeed x = 2: 6·2 = 12 ≡ 3 (mod 9) ✓.',
        explain: 'When gcd(c, n) = d > 1, ax ≡ b (mod n) is solvable iff d | b, and becomes (a/d)x ≡ (b/d) (mod n/d).',
        mistake: 'Dividing only the sides and keeping the modulus — the classic modular error.',
      },
    ],
  },
  {
    id: 'residue-classes',
    title: 'Residue Classes & ℤ/nℤ',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'The n boxes of modular arithmetic: [0], [1], …, [n−1], each the set of integers with that remainder. The set of classes, with addition and multiplication, is the ring ℤ/nℤ.',
    level: 'core',
    csFields: ['cryptography', 'theoretical-cs', 'competitive-programming'],
    prerequisites: ['congruence'],
    related: ['congruence', 'modular-arithmetic-ops', 'finite-fields'],
    next: ['modular-arithmetic-ops'],
    content: [
      { t: 'def', title: 'Residue class', text: '$[a]_n = \\{ a + kn : k \\in \\mathbb{Z} \\}$ is the congruence class of a modulo n. There are exactly n classes: $[0], [1], \\ldots, [n-1]$, and every integer lies in exactly one (the division algorithm).' },
      { t: 'ex', title: 'The classes mod 5', steps: [
        '[0] = {…, −10, −5, 0, 5, 10, …}; [1] = {…, −4, 1, 6, 11, …}; … [4] = {…, −1, 4, 9, 14, …}.',
        'Every integer falls in exactly one: 23 ∈ [3], −7 ∈ [3] (−7 = 5·(−2) + 3).',
        'Addition of classes: [3] + [4] = [2] (3 + 4 = 7 ≡ 2). Multiplication: [3]·[4] = [2] (12 ≡ 2).',
      ] },
      { t: 'p', text: 'The n classes with these operations form the ring $\\mathbb{Z}/n\\mathbb{Z}$ (written ℤ_n). Its units — elements with multiplicative inverses — are exactly the classes [a] with gcd(a, n) = 1; they form the group (ℤ/nℤ)× of size φ(n). When n is prime, every nonzero class is a unit: ℤ_p× has p − 1 elements, and it is this group whose structure Fermat’s and Euler’s theorems describe.' },
      { t: 'cs', items: [
        { area: 'AES', how: 'AES arithmetic runs in the finite field GF(2⁸) — the field version of ℤ/nℤ for n = 256 (with a twist: characteristic 2, not 256). Finite fields are the “prime-modulus” generalization.' },
        { area: 'Hashing', how: 'A hash into n buckets is a map into ℤ_n; good hashes distribute across residue classes uniformly.' },
      ] },
    ],
    practice: [
      { id: 'rcl-p1', q: 'List the residue classes mod 4 and compute [3] + [3] and [3]·[3] in ℤ/4ℤ.', type: 'short', diff: 'easy',
        answer: 'Classes [0],[1],[2],[3]. [3]+[3] = [6] = [2]. [3]·[3] = [9] = [1].',
        explain: 'Work with remainders: 6 mod 4 = 2, 9 mod 4 = 1.',
      },
      { id: 'rcl-p2', q: 'Which classes mod 12 are units (have a multiplicative inverse)?', type: 'short', diff: 'medium',
        answer: '[1],[5],[7],[11] — exactly the classes coprime to 12. φ(12) = 4. Check: 5·5 = 25 ≡ 1; 7·7 = 49 ≡ 1; 11·11 = 121 ≡ 1 (mod 12).',
        explain: 'Units = classes [a] with gcd(a,12) = 1; each inverse is found by extended Euclid.',
      },
    ],
  },
  {
    id: 'modular-arithmetic-ops',
    title: 'Addition, Subtraction & Multiplication Modulo n',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'The working rules of clock arithmetic: reduce whenever, add/multiply freely, and the order of reduction never matters. Plus the standard tricks for big computations.',
    level: 'core',
    csFields: ['competitive-programming', 'cryptography', 'number-theory'],
    prerequisites: ['congruence', 'residue-classes'],
    related: ['modular-inverses', 'linear-congruences', 'modular-exponentiation'],
    next: ['modular-inverses'],
    content: [
      { t: 'props', title: 'Rules of the game', items: [
        { title: 'Reduce early', text: '$(a \\bmod n) + (b \\bmod n) \\equiv a + b \\pmod n$, and likewise for products. You may reduce after every operation — this is how hand computation and code both stay small.' },
        { title: 'Negative remainders', text: '−1 ≡ n−1 (mod n). So −1 mod 7 = 6; in code, watch language conventions (C’s % can be negative, Python’s is nonnegative).' },
        { title: 'Simplify first', text: 'To compute (23·17·31) mod 9: 23 ≡ 5, 17 ≡ 8 ≡ −1, 31 ≡ 4. Product: 5·(−1)·4 = −20 ≡ −2 ≡ 7 (mod 9). Small numbers, same answer.' },
        { title: 'No division without an inverse', text: 'Solve ax ≡ b by inverting a (when gcd(a, n) = 1), or reduce the congruence (see Linear Congruences).' },
      ] },
      { t: 'ex', title: 'A full computation', steps: [
        'Compute 13⁴ + 7 mod 11.',
        '13 ≡ 2 (mod 11), so 13⁴ ≡ 2⁴ = 16 ≡ 5.',
        '7 ≡ 7. Total: 5 + 7 = 12 ≡ 1 (mod 11).',
      ], result: 'Every intermediate stays below n — the discipline that makes modular arithmetic tractable by hand.' },
      { t: 'ex', title: 'Patterns that save time', steps: [
        'If a ≡ ±1: aᵏ ≡ ±1 always.',
        'If a² ≡ 1: all even powers of a are 1 (e.g., 3² = 9 ≡ 1 mod 8, so 3ᵏ ≡ 1 for even k, ≡ 3 for odd k).',
        'Cycles: the powers of any a mod n cycle with period dividing φ(n) (Euler) — find the period, reduce the exponent.',
      ] },
      { t: 'cs', items: [
        { area: 'Competitive programming', how: 'Answers are given mod 10⁹ + 7 (prime); every multiplication in DP is a modular multiply. The modulus is prime so inverses exist for all nonzero values.' },
        { area: 'Checksums', how: 'Sum bytes, take mod 256 (or mod 255 for Luhn-style weighted sums) — a residue-class projection designed to catch errors.' },
      ] },
    ],
    practice: [
      { id: 'mo-p1', q: 'Compute (123 · 456) mod 7 without multiplying fully.', type: 'numeric', diff: 'easy', answer: '4',
        explain: '123 = 7·17 + 4 ≡ 4; 456 = 7·65 + 1 ≡ 1. Product: 4·1 = 4 (mod 7).',
      },
      { id: 'mo-p2', q: 'Compute 5⁸ mod 7.', type: 'numeric', diff: 'easy', answer: '4',
        explain: '5 ≡ −2 (mod 7). 5² ≡ 4, 5⁴ ≡ 16 ≡ 2, 5⁸ ≡ 4 (mod 7).',
      },
      { id: 'mo-p3', q: 'Prove: for all integers a, a³ ≡ a (mod 6).', type: 'proof', diff: 'medium',
        answer: 'Mod 2: a³ − a = a(a−1)(a+1), product of three consecutive integers, even. Mod 3: among a−1, a, a+1 one is divisible by 3, so a³ − a ≡ 0. Since gcd(2,3)=1, a³ − a ≡ 0 (mod 6). (This is Fermat mod 2 and mod 3 combined — a mini-CRT.)',
        explain: 'Prove mod each prime factor of 6, then combine — the standard reduction.',
      },
    ],
  },
  {
    id: 'modular-inverses',
    title: 'Modular Inverses',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'Division modulo n, when it exists: a⁻¹ mod n is the number b with ab ≡ 1 (mod n). It exists exactly when gcd(a, n) = 1, and the extended Euclidean algorithm finds it.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['congruence', 'bezout-identity'],
    related: ['linear-congruences', 'rsa-cryptography', 'modular-exponentiation'],
    content: [
      { t: 'def', title: 'Modular inverse', text: '$a^{-1} \\bmod n$ is the residue class $b$ with $ab \\equiv 1 \\pmod n$. It exists iff $\\gcd(a, n) = 1$, and is then unique modulo n.' },
      { t: 'thm', name: 'Existence & uniqueness', statement: '$a$ has a multiplicative inverse mod n iff $\\gcd(a, n) = 1$.',
        proofTitle: 'Proof',
        proof: [
          'If ax ≡ 1 (mod n), then n | (ax − 1), so ax − ny = 1 for some y — a Bézout combination, forcing gcd(a, n) = 1.',
          'If gcd(a, n) = 1, Bézout gives ax + ny = 1; mod n this is ax ≡ 1, so x is the inverse.',
          'Uniqueness: if ax ≡ 1 and ax′ ≡ 1, then a(x − x′) ≡ 0; multiplying by a⁻¹… or: n | a(x−x′) and gcd(a, n) = 1 give n | (x − x′).',
        ] },
      { t: 'ex', title: 'Inverses in small moduli', steps: [
        '3⁻¹ mod 11: need 3x ≡ 1. Try: 3·4 = 12 ≡ 1 → inverse is 4.',
        '7⁻¹ mod 26: extended Euclid: 26 = 3·7 + 5; 7 = 1·5 + 2; 5 = 2·2 + 1. Back: 1 = 5 − 2·2 = 5 − 2(7 − 5) = 3·5 − 2·7 = 3(26 − 3·7) − 2·7 = 3·26 − 11·7. So 7·(−11) ≡ 1 (mod 26); −11 ≡ 15. Check: 7·15 = 105 = 4·26 + 1 ✓.',
        'No inverse: 6 mod 9 — gcd(6, 9) = 3 ≠ 1. Indeed 6x is always even·… 6x mod 9 ∈ {0, 6, 3} — never 1.',
      ] },
      { t: 'formula', name: 'Fermat shortcut (prime modulus)', latex: 'p \\text{ prime}, a \\not\\equiv 0: \\qquad a^{-1} \\equiv a^{p-2} \\pmod p', note: 'From Fermat: a^{p−1} ≡ 1, so a·a^{p−2} ≡ 1. Computed by repeated squaring in O(log p) — the standard trick in CP when the modulus is prime (e.g. 10⁹+7).' },
      { t: 'cs', items: [
        { area: 'Division in modular DP', how: 'Counting modulo a prime m: divide by k via k^{m−2} mod m — Fermat’s shortcut in every competitive-programming combinatorics solution.' },
        { area: 'RSA', how: 'd = e⁻¹ mod φ(n) — an inverse in a composite modulus, computed by extended Euclid (Fermat’s shortcut does not apply since φ(n) is not prime).' },
      ] },
    ],
    practice: [
      { id: 'mi-p1', q: 'Find 9⁻¹ mod 23.', type: 'numeric', diff: 'medium', answer: '18',
        explain: '9·18 = 162 = 7·23 + 1. (Extended Euclid: 23 = 2·9 + 5; 9 = 1·5 + 4; 5 = 1·4 + 1 → back-substitute: 1 = 5 − 4 = 5 − (9 − 5) = 2·5 − 9 = 2(23 − 2·9) − 9 = 2·23 − 5·9, so 9·(−5) ≡ 1, −5 ≡ 18.)',
      },
      { id: 'mi-p2', q: 'Why does 10 have no inverse mod 100? State the general criterion.', type: 'short', diff: 'easy',
        answer: 'gcd(10, 100) = 10 ≠ 1. Criterion: an inverse exists iff gcd(a, n) = 1.',
        explain: '10x is always divisible by 10, so 10x mod 100 ∈ {0, 10, …, 90} — never 1.',
      },
    ],
  },
  {
    id: 'linear-congruences',
    title: 'Linear Congruences: ax ≡ b (mod n)',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'The fundamental modular equation. Solvable iff gcd(a, n) | b; when solvable, exactly gcd(a, n) solutions modulo n. The bridge from divisibility to equations.',
    level: 'advanced',
    csFields: ['competitive-programming', 'cryptography', 'number-theory'],
    prerequisites: ['modular-inverses', 'congruence'],
    related: ['chinese-remainder-theorem', 'modular-arithmetic-ops'],
    content: [
      { t: 'thm', name: 'Solvability', statement: '$ax \\equiv b \\pmod n$ has solutions iff $d = \\gcd(a, n)$ divides $b$. If solvable, it has exactly $d$ solutions modulo n.',
        proofTitle: 'Proof',
        proof: [
          'ax ≡ b (mod n) ⇔ ax − ny = b for some y (definition of congruence) ⇔ b is a Bézout combination of a, n ⇔ gcd(a, n) | b.',
          'For the count: write a = da′, n = dn′, b = db with gcd(a′, n′) = 1. The congruence becomes a′x ≡ b (mod n′), solvable with the unique solution x₀ mod n′ (invert a′). Lifting back to mod n: the d solutions are x₀, x₀ + n′, x₀ + 2n′, …, x₀ + (d−1)n′.',
        ] },
      { t: 'ex', title: 'Solving 8x ≡ 12 (mod 28)', steps: [
        'd = gcd(8, 28) = 4; 4 | 12, so 4 solutions exist.',
        'Divide by 4: 2x ≡ 3 (mod 7).',
        '2⁻¹ ≡ 4 (mod 7): x ≡ 3·4 = 12 ≡ 5 (mod 7).',
        'Lift: x ≡ 5, 5 + 7, 5 + 14, 5 + 21 (mod 28): {5, 12, 19, 26}.',
        'Check x = 12: 8·12 = 96 = 3·28 + 12 ✓.',
      ] },
      { t: 'callout', kind: 'insight', text: 'The pattern — “divide a, b, n by d; solve the reduced equation; lift d solutions” — is the same pattern that appears in group theory (solving in cyclic groups) and in lattice problems. Master it once, use it forever.' },
      { t: 'cs', items: [
        { area: 'Cyclic scheduling', how: '“Which stations does train k pass, given headway a and cycle n?” is a linear congruence in k.' },
        { area: 'CRS & keys', how: 'Solving for exponents and residues in protocols reduces to systems of linear congruences (and their nonlinear relatives).' },
      ] },
    ],
    practice: [
      { id: 'lc-p1', q: 'Solve 5x ≡ 3 (mod 11).', type: 'short', diff: 'easy',
        answer: '5⁻¹ ≡ 9 (5·9 = 45 ≡ 1). x ≡ 3·9 = 27 ≡ 5 (mod 11). Check: 5·5 = 25 ≡ 3 ✓.',
        explain: 'Invert the coefficient first: 5·9 = 45 = 4·11 + 1, so 5⁻¹ ≡ 9 (mod 11); multiply both sides by it.',
      },
      { id: 'lc-p2', q: 'How many solutions mod 30 does 6x ≡ 9 (mod 30) have? Justify.', type: 'short', diff: 'medium',
        answer: 'Zero solutions. d = gcd(6, 30) = 6, and 6 ∤ 9, so by the solvability criterion the congruence has no solution. Directly: 6x mod 30 ∈ {0, 6, 12, 18, 24} as x ranges — never 9.',
        explain: 'The criterion “d | b” is the whole argument; the quick sanity check is the range of 6x mod 30.',
        mistake: 'Computing gcd(6, 30) as 3 — it is 6 (30 = 5·6).',
      }
    ],
  },
  {
    id: 'modular-exponentiation',
    title: 'Modular Exponentiation & Repeated Squaring',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'Computing aᵉ mod n in O(log e) multiplications by repeated squaring — the single most important subroutine in all of public-key cryptography.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['modular-arithmetic-ops', 'congruence'],
    related: ['fermats-little-theorem', 'rsa-cryptography', 'modular-inverses'],
    content: [
      { t: 'intuition', text: 'Computing aᵉ directly is absurd for e = 1030 (that number has 300 digits). But we only need aᵉ mod n, and aᵉ mod n depends only on e’s binary expansion: aᵉ = a^{e₀ + 2e₁ + 4e₂ + …} = ∏ (a^{2^i})^{e_i}. Precompute the squares a, a², a⁴, a⁸, … (each a squaring), and multiply in the ones-bits of e. Ten hundred bits of exponent → about 1000 squarings, each on a number smaller than n.' },
      { t: 'formula', name: 'The algorithm (right-to-left)', latex: 'r \\leftarrow 1; \\quad \\text{while } e > 0: \\; (\\text{if } e \\text{ odd}) \\; r \\leftarrow r \\cdot a \\bmod n; \\; a \\leftarrow a^2 \\bmod n; \\; e \\leftarrow e / 2', note: 'O(log e) modular multiplications, each on O(log n)-bit numbers.' },
      { t: 'ex', title: 'Compute 3¹³ mod 7', steps: [
        '13 = 1101₂. Squares mod 7: 3¹ = 3; 3² = 9 ≡ 2; 3⁴ ≡ 2² = 4; 3⁸ ≡ 4² = 16 ≡ 2.',
        'Multiply the ones-bits: 3⁸ · 3⁴ · 3⁰? No — 13 = 8 + 4 + 1: result = 3⁸ · 3⁴ · 3¹ ≡ 2 · 4 · 3 = 24 ≡ 3 (mod 7).',
        'Check: 3⁶ ≡ 1 (Fermat, 7 prime), 13 mod 6 = 1, so 3¹³ ≡ 3¹ = 3 ✓.',
      ] },
      { t: 'ex', title: 'Compute 7²⁵ mod 13', steps: [
        '25 = 11001₂ = 16 + 8 + 1. Powers: 7¹ = 7; 7² = 49 ≡ 10; 7⁴ ≡ 10² = 100 ≡ 9; 7⁸ ≡ 81 ≡ 3; 7¹⁶ ≡ 9.',
        'Result: 9 · 3 · 7 = 189 ≡ 189 − 13·14 = 189 − 182 = 7 (mod 13).',
        'Cross-check with Fermat: 7¹² ≡ 1, 25 mod 12 = 1, 7²⁵ ≡ 7 ✓.',
      ] },
      { t: 'cs', items: [
        { area: 'RSA', how: 'Encryption m^e mod n and decryption c^d mod n are both modular exponentiations — the literal core of the protocol.' },
        { area: 'Diffie–Hellman', how: 'Both parties compute g^x mod p (and verify g^{xy}); the exchange is secure because computing x from g^x mod p (discrete log) is believed hard.' },
        { area: 'Competitive programming', how: '“Compute a^b mod m” appears in essentially every number-theory problem set; the O(log b) square-and-multiply template is the expected solution.' },
      ] },
    ],
    practice: [
      { id: 'me-p1', q: 'Compute 2²⁰ mod 31.', type: 'numeric', diff: 'medium', answer: '1',
        explain: 'Fermat: 31 prime, 2³⁰ ≡ 1. 2⁵ = 32 ≡ 1 (mod 31), so 2²⁰ = (2⁵)⁴ ≡ 1.',
      },
      { id: 'me-p2', q: 'How many modular squarings does repeated squaring use for e = 10⁶?', type: 'short', diff: 'medium',
        answer: '20 squarings plus one multiply per set bit of 10⁶ (10⁶ < 2²⁰, so the loop runs 20 times; popcount(10⁶) = 7 multiplies). Total: 27 modular multiplications.',
        explain: 'The loop runs ⌈log₂(e+1)⌉ times; each iteration squares, and multiplies when the current bit is 1.',
      },
    ],
  },
  {
    id: 'chinese-remainder-theorem',
    title: 'The Chinese Remainder Theorem',
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'Simultaneously solving congruences: x ≡ aᵢ (mod nᵢ) for pairwise coprime nᵢ has a unique solution mod (n₁⋯nₖ). “Han Xin counting soldiers” — and a speedup hidden inside RSA.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['linear-congruences', 'modular-inverses'],
    related: ['residue-classes', 'rsa-cryptography'],
    content: [
      { t: 'thm', name: 'Chinese Remainder Theorem (two moduli)', statement: 'If gcd(m, n) = 1, then the system $x \\equiv a \\pmod m$, $x \\equiv b \\pmod n$ has a unique solution modulo $mn$.',
        proofTitle: 'Proof (constructive, via Bézout)',
        proof: [
          'Since gcd(m, n) = 1, Bézout gives um + vn = 1.',
          'Claim: x = a·(vn) + b·(um) solves the system.',
          'Mod m: x ≡ a·(vn) (since um ≡ 0 mod m). And vn = 1 − um ≡ 1 (mod m). So x ≡ a (mod m). ✓',
          'Mod n: x ≡ b·(um) and um ≡ 1 (mod n), so x ≡ b (mod n). ✓',
          'Uniqueness mod mn: if x and x′ both solve, m | (x − x′) and n | (x − x′), and coprime m, n give mn | (x − x′).',
        ] },
      { t: 'ex', title: 'The classic: x ≡ 2 (mod 3), x ≡ 3 (mod 4), x ≡ 1 (mod 5)', steps: [
        'Pairwise coprime moduli 3, 4, 5; product 60.',
        'Combine mod 3 and 4 first: x = 3 + 4k; mod 3 this is k ≡ 2, so k = 2 + 3j and x ≡ 11 (mod 12).',
        'Now x ≡ 11 (mod 12) and x ≡ 1 (mod 5): x = 11 + 12j; mod 5 this is 1 + 2j ≡ 1, so 2j ≡ 0, j ≡ 0 (mod 5).',
        'x ≡ 11 (mod 60). Check: 11 mod 3 = 2 ✓, 11 mod 4 = 3 ✓, 11 mod 5 = 1 ✓. Solution: x ≡ 11 (mod 60).',
      ] },
      { t: 'ex', title: 'Why RSA cares', steps: [
        'RSA decryption computes m = cᵈ mod pq with d ~ the size of n — one big exponentiation.',
        'CRT-RSA: compute m₁ = cᵈ mod p and m₂ = cᵈ mod q (each with exponent ~ log p), then combine by CRT.',
        'Cost drops by a factor of ~4: the square-and-multiply on halves is 4× faster (two n/2-bit ops vs one n-bit op; a k-bit multiply costs ~4× a k/2-bit one).',
        'Every production RSA implementation uses CRT — it is the theorem working in the machine.',
      ] },
      { t: 'analogy', title: 'Think of it like this', text: 'You ask three people each “what is my number modulo 3, 4, 5?” Their three answers pin down your number modulo 60 — because 3, 4, 5 are coprime, the combined question is as informative as asking modulo 60 directly. CRT says: splitting a modulus into coprime pieces loses no information, and computation on the pieces reassembles perfectly.' },
      { t: 'cs', items: [
        { area: 'Parallel computation', how: 'A huge mod-n exponentiation can be split across cores working mod the coprime factors, then merged — literal parallelism from a 1700s theorem.' },
        { area: 'Secret sharing', how: 'Some threshold secret-sharing schemes distribute shares as congruence classes that only recombine via CRT.' },
      ] },
    ],
    practice: [
      { id: 'crt-p1', q: 'Solve x ≡ 1 (mod 2), x ≡ 2 (mod 3), x ≡ 3 (mod 4).', type: 'short', diff: 'medium',
        answer: 'x ≡ 11 (mod 12). Combine mod 2 and 3 first: x = 2 + 3k; mod 2: 2 + 3k ≡ k ≡ 1 → k ≡ 1 → x ≡ 5 (mod 6). Now x ≡ 5 (mod 6), x ≡ 3 (mod 4): x = 5 + 6j; mod 4: 1 + 2j ≡ 3 → 2j ≡ 2 → j ≡ 1 (mod 2). x ≡ 11 (mod 12). Check: 11 mod 2 = 1 ✓, 11 mod 3 = 2 ✓, 11 mod 4 = 3 ✓.',
        explain: 'Pair the congruences two at a time; the modulus grows to the product (here 2·3·4… note 2 and 4 are not coprime — but x ≡ 1 mod 2 is implied by x ≡ 3 mod 4, so the effective moduli 3 and 4 are coprime and the solution is unique mod 12).',
      },
      { id: 'crt-p2', q: 'Why must the moduli be pairwise coprime for uniqueness? Give a failing example.', type: 'short', diff: 'medium',
        answer: 'Uniqueness mod mn requires mn | (x−x′) from m | and n |, which needs gcd(m, n) = 1. Failing example: x ≡ 1 (mod 2), x ≡ 0 (mod 4) has no solution at all (1 is odd, 0 mod 4 is even). Even when solvable, non-coprime moduli give a solution mod lcm(m,n) with redundancy, not a clean unique class mod mn.',
        explain: 'Coprimality is what makes the two pieces of information independent.',
      },
    ],
  },
  {
    id: 'euler-totient',
    title: "Euler's Totient Function φ(n)",
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'φ(n) counts the integers 1 ≤ k ≤ n that are coprime to n — the size of the unit group (ℤ/nℤ)×. Multiplicative, with a product formula, and the engine of Euler’s theorem.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['residue-classes'],
    related: ['euler-theorem', 'rsa-cryptography', 'modular-inverses'],
    content: [
      { t: 'def', title: 'Totient function', text: '$\\varphi(n)$ is the count of integers $1 \\le k \\le n$ with $\\gcd(k, n) = 1$ (equivalently, the number of units in $\\mathbb{Z}/n\\mathbb{Z}$).' },
      { t: 'formula', name: 'Product formula', latex: 'n = p_1^{e_1} \\cdots p_r^{e_r} \\;\\Rightarrow\\; \\varphi(n) = n \\prod_{i=1}^{r} \\left(1 - \\frac{1}{p_i}\\right) = \\prod_{i=1}^{r} p_i^{e_i - 1}(p_i - 1)', note: 'Inclusion–exclusion: the fraction of integers NOT divisible by pᵢ is 1 − 1/pᵢ; the events are independent across distinct primes.' },
      { t: 'thm', name: 'Multiplicativity', statement: 'If gcd(m, n) = 1, then φ(mn) = φ(m)φ(n).',
        proofTitle: 'Proof (sketch)',
        proof: [
          'The CRT gives a bijection ℤ/mnℤ ≅ ℤ/mℤ × ℤ/nℤ.',
          'A class is a unit mod mn iff it is a unit mod m and mod n (a prime dividing mn divides exactly one of m, n).',
          'So units correspond to pairs of units: φ(mn) = φ(m)φ(n).',
        ] },
      { t: 'ex', title: 'Computing φ', steps: [
        'φ(12) = 12(1−1/2)(1−1/3) = 12·½·⅔ = 4. (Coprime to 12 in [1,12]: 1, 5, 7, 11. ✓)',
        'φ(100) = 100(1−1/2)(1−1/5) = 40. (Coprimes to 100: the 1–99 not divisible by 2 or 5.)',
        'φ(p) = p − 1 for prime p — every nonzero residue is a unit.',
        'φ(2p) = φ(2)φ(p) = (p−1) for odd prime p.',
      ] },
      { t: 'cs', items: [
        { area: 'RSA', how: 'φ(n) = (p−1)(q−1) is the modulus in which d inverts e — computing φ(n) requires knowing p and q (that is the private part of the key).' },
        { area: 'Order of elements', how: 'The multiplicative order of a mod n divides φ(n) (Lagrange) — the backbone of discrete-log security parameters. Choose primes p with p−1 having a large prime factor so that (ℤ/pℤ)× has a large-order subgroup.' },
      ] },
    ],
    practice: [
      { id: 'et-p1', q: 'Compute φ(360).', type: 'numeric', diff: 'medium', answer: '96',
        explain: '360 = 2³·3²·5. φ = 360·(1/2)(2/3)(4/5) = 360·8/30 = 96.',
      },
      { id: 'et-p2', q: 'Prove: for prime p, the equation x² ≡ 1 (mod p) has exactly two solutions, ±1.', type: 'proof', diff: 'hard',
        answer: 'x² − 1 = (x−1)(x+1) ≡ 0 (mod p). Since p is prime, ℤ/pℤ is an integral domain: no zero divisors, so p | (x−1) or p | (x+1). Hence x ≡ 1 or −1. Both are solutions. Exactly two.',
        explain: '“No zero divisors” is the prime-modulus superpower — it fails mod composites (e.g. x² ≡ 1 mod 8 has 4 solutions: 1, 3, 5, 7), which is exactly what Miller–Rabin exploits.',
      },
    ],
  },
  {
    id: 'fermats-little-theorem',
    title: "Fermat's Little Theorem",
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'For prime p and a not divisible by p: a^(p−1) ≡ 1 (mod p). A three-line statement with a four-hundred-year career: powering in prime fields, primality testing, and the correctness of RSA all rest on it.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory', 'theoretical-cs'],
    prerequisites: ['congruence', 'modular-exponentiation', 'euler-totient', 'primes', 'divisibility'],
    related: ['euler-theorem', 'primality-testing', 'rsa-cryptography'],
    content: [
      { t: 'h', text: 'Why it matters (motivation)' },
      { t: 'p', text: 'Exponents are the hard part of modular arithmetic: aᵉ mod p with e in the millions is infeasible to expand. Fermat says that modulo a prime, exponents repeat with period dividing p − 1 — so you can reduce huge exponents by p − 1 before computing. This single fact underlies fast modular exponentiation in prime fields, the Fermat and Miller–Rabin primality tests, and (via Euler’s generalization) the correctness proof of RSA.' },
      { t: 'h', text: 'Statement' },
      { t: 'thm', name: "Fermat's Little Theorem", statement: 'If p is prime and a is an integer with p ∤ a, then $a^{p-1} \\equiv 1 \\pmod p$. Equivalently, for all a: $a^p \\equiv a \\pmod p$.',
        proofTitle: 'Proof (the permutation argument)',
        proof: [
          'Consider the p − 1 numbers a·1, a·2, …, a·(p−1) modulo p.',
          'None is 0 mod p (p ∤ a, and p ∤ k for 1 ≤ k ≤ p−1, and p is prime).',
          'They are all distinct: a·i ≡ a·j (mod p) ⇒ a(i−j) ≡ 0 (mod p) ⇒ p | (i−j) (since p ∤ a) ⇒ i = j, as 1 ≤ i, j ≤ p−1.',
          'So {a·1, …, a·(p−1)} is a permutation of {1, 2, …, p−1} modulo p.',
          'Multiply: a^{p−1}·(p−1)! ≡ (p−1)! (mod p). Since p ∤ (p−1)! (no factor of p in the product), we may “divide” by (p−1)! (it is a unit): a^{p−1} ≡ 1 (mod p). ∎',
        ] },
      { t: 'h', text: 'Intuition' },
      { t: 'p', text: 'Multiplication by a (mod p) is a shuffle of the nonzero residues — a permutation that rotates the “multiplicative clock” without collapsing it. Going around the full circle of p − 1 steps must return to where you started, up to the starting point itself: that is exactly a^{p−1} ≡ 1. In group language: the nonzero residues form a group of order p − 1, and every element’s (p−1)-st power is the identity (Lagrange’s theorem in its simplest case).' },
      { t: 'h', text: 'Examples' },
      { t: 'ex', title: 'Small cases', steps: [
        'p = 7, a = 3: 3⁶ = 729 = 104·7 + 1 ≡ 1 (mod 7). ✓',
        'p = 5, a = 2: 2⁴ = 16 ≡ 1 (mod 5). ✓',
        'p = 11, a = 2: 2¹⁰ = 1024 = 93·11 + 1 ≡ 1. ✓',
        'Equivalent form: 3⁷ ≡ 3 (mod 7) since 3⁷ = 3·3⁶ ≡ 3·1 = 3.',
      ] },
      { t: 'ex', title: 'Reducing a huge exponent', steps: [
        'Compute 2¹⁰⁰ mod 13. Fermat: 2¹² ≡ 1, and 100 = 8·12 + 4.',
        'So 2¹⁰⁰ = (2¹²)⁸·2⁴ ≡ 1⁸·16 ≡ 3 (mod 13).',
        'A 100-bit exponent reduced to a 4-bit computation — the practical heart of the theorem.',
      ] },
      { t: 'ex', title: 'A failed converse (why primes are special)', steps: [
        'Try a = 2, n = 341 = 11·31 (composite): 2³⁴⁰ mod 341 = 1 — it passes the Fermat test anyway!',
        'Such composites are Carmichael numbers (341, 561, 645, …); infinitely many exist.',
        'Lesson: “a^{n−1} ≡ 1” is a property ALL primes have, but some composites share it. Primes ⇒ the congruence; the congruence ⇏ prime.',
      ] },
      { t: 'h', text: 'Alternative understanding: residue classes' },
      { t: 'p', text: 'In ℤ/pℤ the nonzero classes form a cyclic-ish group of order p − 1 (cyclic in fact). The theorem is the statement that raising every element to the group order lands on the identity: the “clock of residues” has exactly p − 1 ticks. For a generator g, the powers g, g², …, g^{p−1} run through all nonzero classes and close the loop at 1 — the loop-closing is the theorem.' },
      { t: 'h', text: 'Applications' },
      { t: 'cs', items: [
        { area: 'Modular exponentiation', how: 'Exponent reduction mod (p−1) makes prime-field powering fast — the subroutine behind RSA, DH, and elliptic-curve scalar multiplication (with the group order playing p−1’s role).' },
        { area: 'Primality testing', how: 'The Fermat test: pick random a, check a^{n−1} ≡ 1 (mod n). All primes pass; composites mostly fail; Carmichael numbers are the exception that motivated Miller–Rabin.' },
        { area: 'Modular inverses', how: 'a⁻¹ ≡ a^{p−2} (mod p) — compute inverses by exponentiation (O(log p) squarings) instead of extended Euclid; standard in finite-field code.' },
        { area: 'RSA correctness', how: 'RSA uses the generalization (Euler’s theorem) a^{φ(n)} ≡ 1 (mod n); the p and q moduli each satisfy Fermat, and CRT stitches the two halves into decryption correctness.' },
        { area: 'Counting in CP', how: 'Dividing by k mod prime p: multiply by k^{p−2}. Every combinatorics modulo 10⁹+7 solution uses Fermat for division.' },
      ] },
      { t: 'viz', id: 'modular-clock', props: { modulus: 7, showPowers: 3 } },
      { t: 'analogy', title: 'Think of it like this', text: 'Walk around a 7-person circle starting at the front desk, taking steps of size 3: you visit 3, 6, 2, 5, 1, 4, 1, … — six steps visit everyone exactly once, and the seventh step lands you home. Fermat says this “visit everyone exactly once” happens for any step size coprime to a prime circle. The p − 1 steps close the loop: that is a^{p−1} ≡ 1.' },
    ],
    practice: [
      { id: 'flt-p1', q: 'Compute 5¹⁰⁰ mod 11 using Fermat’s Little Theorem.', type: 'numeric', diff: 'easy', answer: '1',
        explain: '10⁰⁰… 100 mod 10 = 0 (since 11 prime, period 10): 5¹⁰⁰ = (5¹⁰)¹⁰ ≡ 1 (mod 11).',
      },
      { id: 'flt-p2', q: 'Compute 7²⁰²⁵ mod 17.', type: 'numeric', diff: 'medium', answer: '10. Reduce the exponent: 2025 mod 16 = 9 (16·126 = 2016). Then 7⁹ mod 17: 7² = 49 ≡ −2; 7⁴ ≡ 4; 7⁸ ≡ 16 ≡ −1; 7⁹ = 7⁸·7 ≡ −7 ≡ 10 (mod 17)',
        explain: 'Fermat reduces 2025 mod 16 to 9; then repeated squaring gives 7⁹ ≡ 10 (mod 17).',
        mistake: 'The listed “15 (−2)” is wrong; the correct residue is 10. (7⁹ ≡ 10 mod 17.)',
      },
      { id: 'flt-p3', q: 'Prove the equivalent form: a^p ≡ a (mod p) for all integers a.', type: 'proof', diff: 'medium',
        answer: 'If p | a, then a ≡ 0 and a^p ≡ 0 ≡ a. If p ∤ a, FLT gives a^{p−1} ≡ 1, so a^p = a·a^{p−1} ≡ a. Both cases covered.',
        explain: 'The “for all a” form absorbs the coprime hypothesis into the zero case.',
      },
      { id: 'flt-p4', q: 'A composite n passes the Fermat test for base a = 2 (2^{n−1} ≡ 1 mod n). Can you conclude n is prime? Justify with an explicit example.', type: 'mcq', diff: 'hard',
        options: ['Yes, that is the theorem', 'No — 341 = 11·31 passes with base 2', 'Only if n < 1000', 'Only with base a = 3'], correct: 1,
        explain: '341 is the smallest base-2 pseudoprime: 2³⁴⁰ ≡ 1 (mod 341) despite 341 = 11·31. Carmichael numbers pass for every coprime base. The theorem is one-directional.',
      },
    ],
  },
  {
    id: 'euler-theorem',
    title: "Euler's Theorem",
    domain: 'number-theory',
    parent: 'modular-arithmetic',
    summary: 'The generalization of Fermat to composite moduli: if gcd(a, n) = 1 then a^φ(n) ≡ 1 (mod n). The theorem that actually powers RSA correctness.',
    level: 'advanced',
    csFields: ['cryptography', 'competitive-programming', 'number-theory'],
    prerequisites: ['euler-totient', 'fermats-little-theorem'],
    related: ['rsa-cryptography', 'modular-exponentiation'],
    content: [
      { t: 'thm', name: "Euler's theorem", statement: 'If $\\gcd(a, n) = 1$, then $a^{\\varphi(n)} \\equiv 1 \\pmod n$.',
        proofTitle: 'Proof (same permutation argument)',
        proof: [
          'Let S = {r₁, …, r_{φ(n)}} be the units mod n (the classes coprime to n).',
          'Multiplication by a permutes S: a·rᵢ are distinct units (a unit times a unit is a unit; distinctness by cancellation, which holds for units).',
          'Multiply all elements: a^{φ(n)}·(r₁⋯r_{φ(n)}) ≡ r₁⋯r_{φ(n)} (mod n).',
          'The product R = r₁⋯r_{φ(n)} is itself a unit, so cancel it: a^{φ(n)} ≡ 1 (mod n). ∎',
        ] },
      { t: 'p', text: 'Fermat is the special case n = p (prime), where φ(p) = p − 1. Euler’s theorem replaces “p − 1” by “φ(n)” — the size of the unit group — and is the form RSA actually uses, since n = pq is composite.' },
      { t: 'ex', title: 'Euler in action', steps: [
        'Compute 2⁸ mod 15: φ(15) = 8, gcd(2,15) = 1, so 2⁸ ≡ 1 (mod 15). ✓ (256 = 17·15 + 1.)',
        'Compute 7²⁰ mod 100: φ(100) = 40, 20 < 40 so no reduction; compute 7² = 49, 7⁴ ≡ 49² = 2401 ≡ 1 (mod 100)! So 7²⁰ = (7⁴)⁵ ≡ 1 (mod 100). (The order 4 divides φ(100) = 40 — orders always divide φ(n).)',
      ] },
      { t: 'callout', kind: 'insight', text: 'What RSA really uses is a stronger fact: for n = pq, a^{φ(n)} ≡ 1 (mod n) holds for ALL a (not just coprime ones) — provable by checking the p and q cases separately and applying CRT. This is why RSA decryption works even when the message m shares a factor with n (a negligible event for random m).' },
      { t: 'cs', items: [
        { area: 'RSA', how: 'Correctness: c^{ed} = m^{ed} ≡ m^{1 + kφ(n)} ≡ m·(m^{φ(n)})^k ≡ m (mod n) — Euler, once, is the whole decryption proof (plus the all-a strengthening).' },
        { area: 'Discrete log parameters', how: 'The order of any unit divides φ(n); choosing n with large-prime φ(n) (or p − 1) makes discrete log hard — parameter selection is φ-arithmetic.' },
      ] },
    ],
    practice: [
      { id: 'eut-p1', q: 'Compute φ(210) and verify a^{φ(210)} ≡ 1 (mod 210) for a = 2.', type: 'short', diff: 'medium',
        answer: '210 = 2·3·5·7. φ(210) = 210·(1/2)(2/3)(4/5)(6/7) = 48. But gcd(2, 210) = 2 ≠ 1, so Euler’s theorem does not apply to a = 2 — the word “verify” is a trap. Indeed 2⁴⁸ is even, hence ≡ 0 (mod 2), and so 2⁴⁸ ≢ 1 (mod 210). For a unit such as a = 7: 7⁴⁸ ≡ 1 (mod 210) ✓ (check mod 2, 3, 5, 7 and combine by CRT).',
        explain: 'Always check gcd(a, n) = 1 before applying Euler — the hypothesis is not decorative.',
        mistake: 'Applying the theorem to a non-unit; the conclusion is false (2⁴⁸ ≢ 1 mod 210 since it is even).',
      },
      { id: 'eut-p2', q: 'Show that the multiplicative order of a mod n (smallest k with aᵏ ≡ 1) always divides φ(n).', type: 'proof', diff: 'hard',
        answer: 'Write φ(n) = qk + r with 0 ≤ r < k (division algorithm on the order). a^{φ(n)} = a^{qk+r} = (aᵏ)^q·aʳ ≡ aʳ. Euler gives a^{φ(n)} ≡ 1, so aʳ ≡ 1 with 0 ≤ r < k — minimality of k forces r = 0, so k | φ(n).',
        explain: 'The same “reduce the exponent by the period” argument that makes Fermat useful is a proof in disguise.',
      },
    ],
  },
];

export const ntRsa: Concept[] = [
  {
    id: 'rsa-cryptography',
    title: 'RSA: Public-Key Cryptography',
    domain: 'number-theory',
    parent: undefined,
    topic: true,
    summary: 'The complete pipeline: pick primes p, q; publish n = pq; encrypt with m^e mod n; decrypt with m^d mod n — where ed ≡ 1 (mod φ(n)). Number theory, assembled into a working encryption system.',
    level: 'advanced',
    csFields: ['cryptography', 'cybersecurity', 'blockchain', 'number-theory'],
    prerequisites: ['primes', 'euler-totient', 'fermats-little-theorem', 'modular-exponentiation', 'chinese-remainder-theorem'],
    related: ['euler-theorem', 'modular-inverses', 'primality-testing', 'chinese-remainder-theorem'],
    content: [
      { t: 'h', text: 'Why it works: the two-way trapdoor' },
      { t: 'p', text: 'RSA is a **trapdoor one-way function**: easy to compute forward (raise to e, mod n), easy to invert if you know the secret (raise to d), hard to invert without it. The hardness assumption is **integer factorization**: given n = pq, finding p and q (and hence φ(n) and d) is believed infeasible for large n — no efficient classical algorithm is known (GNFS is subexponential, but infeasible at 2048-bit scale; a large quantum computer would break it via Shor’s algorithm).' },
      { t: 'h', text: 'Key generation' },
      { t: 'ex', title: 'Keys (the standard educational example)', steps: [
        '1. Pick distinct primes: p = 61, q = 53.',
        '2. n = p·q = 3233. (This is the public modulus.)',
        '3. φ(n) = (p−1)(q−1) = 60·52 = 3120.',
        '4. Choose e with 1 < e < φ(n) and gcd(e, φ(n)) = 1: e = 17 (works; also 3, 5, …).',
        '5. Compute d = e⁻¹ mod φ(n) by extended Euclid: 17·2753 = 46801 = 15·3120 + 1, so d = 2753.',
        'Public key: (n, e) = (3233, 17). Private key: d = 2753.',
      ] },
      { t: 'h', text: 'Encrypt & decrypt' },
      { t: 'ex', title: 'A full round trip', steps: [
        'Message m = 65 (0 ≤ m < n).',
        'Encrypt: c = mᵉ mod n = 65¹⁷ mod 3233 = 2790 (by repeated squaring).',
        'Anyone with the public key can do this; that is the point.',
        'Decrypt: m′ = cᵈ mod n = 2790²⁷⁵³ mod 3233 = 65 = m. ✓',
        'Only the holder of d can do this efficiently (assumed).',
      ] },
      { t: 'h', text: 'Correctness proof' },
      { t: 'thm', name: 'RSA decrypts correctly', statement: 'For 0 ≤ m < n = pq, with ed ≡ 1 (mod φ(n)): (mᵉ)ᵈ ≡ m (mod n).',
        proofTitle: 'Proof',
        proof: [
          'ed = 1 + kφ(n) for some k. So (mᵉ)ᵈ = m^{ed} = m·(m^{φ(n)})^k.',
          'If gcd(m, n) = 1: Euler gives m^{φ(n)} ≡ 1 (mod n), so m^{ed} ≡ m·1^k ≡ m (mod n).',
          'If p | m (the other case, since m < pq): then m ≡ 0 (mod p), so m^{ed} ≡ 0 ≡ m (mod p). Mod q: m is invertible, ed ≡ 1 (mod φ(n)) implies ed ≡ 1 (mod q−1) (as q−1 | φ(n)), so by Fermat m^{ed} ≡ m (mod q).',
          'A number ≡ m mod p and ≡ m mod q is ≡ m mod pq = n by CRT. Done — for every m, coprime or not.',
        ] },
      { t: 'h', text: 'Security, in one paragraph' },
      { t: 'p', text: 'Breaking RSA (finding d from (n, e)) requires φ(n) = n − (p+q) + 1, i.e., p + q, i.e., the factorization of n. So the security reduces to: **factoring a 2048-bit (617-digit) integer is infeasible classically**. The best algorithm, the General Number Field Sieve, runs in time exp(( (64/9)^{1/3} + o(1) ) (ln n ln ln n)^{1/3} ) — subexponential, but at 2048 bits this is far beyond any known compute. Quantum computers run Shor’s algorithm in polynomial time — the standing reason for post-quantum migration. Note also: RSA is used for **encryption of small blocks** (hybrid with AES for bulk data) and, more commonly, for **digital signatures** (sign h(m) with s = h(m)^d mod n; verify sᵉ ≡ h(m)).' },
      { t: 'callout', kind: 'warning', text: 'Never use small primes, never reuse e across messages with small exponents and no padding (Håstad’s broadcast attack), and use proper padding schemes (OAEP) in production. Textbook RSA is the mathematics; real RSA is textbook RSA plus padding against known attacks.' },
      { t: 'viz', id: 'rsa', props: { p: 61, q: 53, m: 65 } },
      { t: 'analogy', title: 'Think of it like this', text: 'RSA is a mailbox with a lock that has no keyhole for inserting: anyone can put a letter in (encrypt with the public lock), but only the person holding the special cutter (the private key, which requires knowing how the mailbox is built — p and q) can open it. The cutter cannot be made from the outside view of the mailbox (n) without knowing the internal construction.' },
      { t: 'cs', items: [
        { area: 'TLS/HTTPS', how: 'RSA (or its successors) authenticates the server and establishes the session key at the start of every secure connection.' },
        { area: 'Digital signatures', how: 'Email signing (PGP), code-signing, and blockchains sign hashes with RSA-style exponentiation; verification is a single public exponentiation.' },
        { area: 'Diffie–Hellman cousin', how: 'The same modular-exponentiation engine, with security from the discrete log instead of factoring — the two protocols share the arithmetic, differ in the hardness assumption.' },
      ] },
    ],
    practice: [
      { id: 'rsa-p1', q: 'With p = 11, q = 13, e = 7: compute n, φ(n), d, and the encryption of m = 9.', type: 'short', diff: 'medium',
        answer: 'n = 143, φ(n) = 120. Need d: 7d ≡ 1 mod 120. 7·103 = 721 = 6·120 + 1 → d = 103. Encrypt: 9⁷ mod 143. 9² = 81; 9⁴ = 81² = 6561 mod 143: 143·45 = 6435, 6561−6435 = 126. 9⁷ = 9⁴·9²·9 = 126·81·9 = 126·729. 729 mod 143: 143·5 = 715, 729−715 = 14. 126·14 = 1764; 143·12 = 1716; 1764−1716 = 48. So c = 48. Decrypt check: 48¹⁰³ mod 143 = 9 (trust the theorem / verify by code).',
        explain: 'The full pipeline on small numbers: factor → φ → invert e → square-and-multiply. c = 48.',
      },
      { id: 'rsa-p2', q: 'Why does ed ≡ 1 (mod φ(n)) guarantee decryption, but ed ≡ 1 (mod n) would not be the right condition?', type: 'short', diff: 'hard',
        answer: 'Decryption needs m^{ed} ≡ m (mod n) for all m, which follows from m^{φ(n)} ≡ 1 (mod n) for units (Euler) — the exponent 1 must be a multiple relation mod φ(n), the order-dividing number. Using mod n would not ensure m^{ed−1} ≡ 1; the multiplicative structure lives in the unit group of size φ(n), not in the ring of size n. (The correct statement: ed ≡ 1 mod λ(n), the Carmichael function, also suffices and is the sharpest condition; φ(n) is the standard, easily computed choice.)',
        explain: 'The relevant “clock size” for exponentiation is the group order φ(n) (or λ(n)), not n.',
      },
      { id: 'rsa-p3', q: 'An adversary has n = 3233 and e = 17, and has factored n = 61·53. Recover d.', type: 'numeric', diff: 'easy', answer: '2753',
        explain: 'φ(n) = 60·52 = 3120; d = 17⁻¹ mod 3120 = 2753 (17·2753 ≡ 1 mod 3120). Factoring is the whole game.',
      },
    ],
  },
  {
    id: 'hashing-number-theory',
    title: 'Hashing & Number Theory',
    domain: 'number-theory',
    parent: undefined,
    summary: 'How number theory enters hashing: modular reduction, prime moduli, polynomial rolling hashes, and the birthday bound on collisions — the mathematics behind every hash table and content-addressed store.',
    level: 'core',
    csFields: ['algorithms-dsa', 'cybersecurity', 'blockchain', 'competitive-programming'],
    prerequisites: ['congruence', 'modular-arithmetic-ops', 'primes'],
    related: ['congruence', 'primes', 'probability-axioms'],
    content: [
      { t: 'intuition', text: 'A hash function h maps a huge universe of keys to a small set of buckets {0, …, m−1} — which is literally a map into ℤ/mℤ. The number-theoretic questions: how to choose m so keys spread uniformly? how to make h fast and hard to attack? how many keys can we expect before a collision? Each has a clean mathematical answer.' },
      { t: 'h', text: 'Modular reduction and prime moduli' },
      { t: 'p', text: 'The simplest hash: h(k) = k mod m. Its quality depends on m’s relation to the key structure: if keys are multiples of d and m shares a factor with d, entire families of keys alias into the same buckets. Choosing **m prime** (or a power of two with a good bit-mixing hash) kills this: for a prime m, any arithmetic progression of keys with common difference not divisible by m wraps uniformly around all buckets. This is why table sizes in “hash = (a·k + b) mod m” designs are prime — number theory in the middle of a data structure.' },
      { t: 'h', text: 'Polynomial (rolling) hashes' },
      { t: 'formula', name: 'Substring hash', latex: 'h(s) = \\sum_{i} s_i \\, B^{i} \\bmod M', note: 'With base B and large modulus M (prime). The shift identity h(s[1..n+1]) = (h(s[0..n−1])·B + s_{n+1}) mod M makes sliding-window hashing O(1) per step — the engine of Rabin–Karp string matching.' },
      { t: 'ex', title: 'Rabin–Karp in one line', steps: [
        'Pattern p of length m, text t of length n. Compute h(p) once.',
        'Slide a window: update the window hash by removing the front character’s contribution (×B^{m−1}) and adding the new one — O(1) per position.',
        'On hash match, verify character-by-character (hash collisions are possible — the verification step is why Rabin–Karp is exact).',
      ], result: 'Expected O(n + m) time with a good random base — number theory (modular arithmetic) doing the algorithm’s work.' },
      { t: 'h', text: 'The birthday bound' },
      { t: 'thm', name: 'Collision probability (birthday bound)', statement: 'With m buckets and k random keys, the collision probability is ≈ 1 − e^{−k(k−1)/(2m)}; it reaches ≈ 0.63 when k ≈ √(2m ln 2) ≈ 1.18√m.',
        proof: [
          'P(no collision) = ∏_{i=1}^{k−1} (1 − i/m) ≈ exp(−∑ i/m) = exp(−k(k−1)/(2m)).',
          'Set the exponent to −ln 2: k ≈ √(2m ln 2).',
          'For a 128-bit hash (m = 2¹²⁸), a birthday attack needs ~2⁶⁴ evaluations — the reason hash security is quoted in “birthday bits” (64 for 128-bit digests), not in digest length.',
        ] },
      { t: 'cs', items: [
        { area: 'Hash tables', how: 'Load-factor and resize analysis is birthday arithmetic: at 0.7 load in an m-bucket table, the expected cluster behavior follows from the same sums.' },
        { area: 'Blockchain', how: 'A 256-bit hash (SHA-256) has 128 bits of collision security — the birthday bound applied to the protocol’s security level.' },
        { area: 'Universal hashing', how: 'Random a, b from a large prime field: h(k) = (ak + b) mod p mod m is a universal family — collision probability ≤ 1/m, proven by counting in the field (number theory again).' },
      ] },
    ],
    practice: [
      { id: 'hn-p1', q: 'A hash table has m = 1000 buckets. About how many keys give a ~50% chance of at least one collision?', type: 'numeric', diff: 'medium', answer: '≈ 38 (1.18·√1000 ≈ 37.3)',
        explain: 'Birthday bound: k ≈ 1.18√m. For m = 1000: √1000 ≈ 31.6, ×1.18 ≈ 37.3. (Exact: the 50% point is around k = 38.)',
      },
      { id: 'hn-p2', q: 'Keys arrive as multiples of 5. If the table size is m = 100, what fraction of buckets can the hash h(k) = k mod 100 ever hit?', type: 'short', diff: 'easy',
        answer: 'k = 5j → h = 5j mod 100 ∈ {0, 5, 10, …, 95}: only 20 buckets — 20% of the table. A prime size (or a mixed hash) fixes this: 5 is invertible mod a prime p, so multiples of 5 wrap uniformly.',
        explain: 'Aliasing from shared factors — the exact reason prime table sizes are used in arithmetic hashing.',
      },
    ],
  },
];

export const numberTheory2: Concept[] = [
  ...ntGcdLcm,
  ...ntModular,
  ...ntRsa,
];
